---
phase: 03-forms-and-lead-capture
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/messages/tr.json
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/ru.json
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/__tests__/form-actions.test.ts
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04
  - FORM-05

must_haves:
  truths:
    - "All form labels, placeholders, validation errors, and success/error messages display correctly in TR, EN, FR, and RU"
    - "Navigation includes links to form pages (contact always visible, others discoverable)"
    - "Server actions correctly call Resend with form data (verified by mocked test)"
    - "Form pages render with translated content in all 4 locales"
  artifacts:
    - path: "src/messages/tr.json"
      provides: "Turkish translations for Forms, Validation, QuotePage, SourcingPage, CallbackPage"
      contains: "Forms"
    - path: "src/messages/en.json"
      provides: "English translations for Forms, Validation, QuotePage, SourcingPage, CallbackPage"
      contains: "Forms"
    - path: "src/messages/fr.json"
      provides: "French translations for Forms, Validation, QuotePage, SourcingPage, CallbackPage"
      contains: "Forms"
    - path: "src/messages/ru.json"
      provides: "Russian translations for Forms, Validation, QuotePage, SourcingPage, CallbackPage"
      contains: "Forms"
    - path: "src/__tests__/form-actions.test.ts"
      provides: "Server action tests with mocked Resend"
      min_lines: 50
  key_links:
    - from: "src/components/forms/contact-form.tsx"
      to: "src/messages/en.json"
      via: "useTranslations('Forms.contact')"
      pattern: "Forms\\.contact"
    - from: "src/lib/actions/contact.ts"
      to: "src/messages/en.json"
      via: "getTranslations('Forms')"
      pattern: "Forms"
    - from: "src/components/layout/header.tsx"
      to: "form pages"
      via: "navigation links"
      pattern: "/quote|/sourcing|/callback"
---

<objective>
Add all i18n translations for forms and validation messages in 4 languages, update site navigation to link to form pages, and create server action integration tests.

Purpose: Complete the forms feature by ensuring all text is properly translated, forms are discoverable via navigation, and the email notification pipeline is verified by tests.
Output: Updated translation files (4 locales), updated header/footer navigation, server action tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forms-and-lead-capture/03-RESEARCH.md
@.planning/phases/03-forms-and-lead-capture/03-01-SUMMARY.md
@.planning/phases/03-forms-and-lead-capture/03-02-SUMMARY.md

@src/messages/en.json
@src/messages/tr.json
@src/components/layout/header.tsx
@src/components/layout/footer.tsx

<interfaces>
<!-- Translation keys needed by form components -->

Form components use these translation namespaces:
- `Forms.contact.{fieldLabel}` - Contact form field labels and placeholders
- `Forms.quote.{fieldLabel}` - Quote form field labels and placeholders
- `Forms.sourcing.{fieldLabel}` - Sourcing form field labels and placeholders
- `Forms.callback.{fieldLabel}` - Callback form field labels and placeholders
- `Forms.success` - Generic success message
- `Forms.errors.rateLimited` - Rate limit error
- `Forms.errors.recaptchaFailed` - reCAPTCHA failure
- `Forms.errors.validationFailed` - Validation failure
- `Forms.errors.sendFailed` - Email send failure
- `Validation.{fieldName}.{errorType}` - Field-specific validation errors
- `QuotePage.{title|subtitle|...}` - Quote page text
- `SourcingPage.{title|subtitle|...}` - Sourcing page text
- `CallbackPage.{title|subtitle|...}` - Callback page text
- `ContactPage.responseTime` - Response time note
- `Common.whatsappChat` - WhatsApp button aria-label

Server Actions use:
- `Forms.errors.*` and `Forms.success` via getTranslations('Forms')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all form and validation translations to all 4 locale files</name>
  <files>
    src/messages/tr.json,
    src/messages/en.json,
    src/messages/fr.json,
    src/messages/ru.json
  </files>
  <action>
    Add the following translation key groups to ALL 4 locale JSON files. Read each file first, then add the new keys while preserving all existing content.

    **Key groups to add:**

    1. **`Forms` namespace** -- shared form messages and per-form labels:

    ```json
    "Forms": {
      "success": "Your message has been sent successfully. We will get back to you soon.",
      "errors": {
        "rateLimited": "Too many requests. Please try again in a minute.",
        "recaptchaFailed": "Security verification failed. Please try again.",
        "validationFailed": "Please check the form for errors.",
        "sendFailed": "Failed to send message. Please try again later."
      },
      "contact": {
        "title": "Send us a Message",
        "name": "Full Name",
        "namePlaceholder": "Your full name",
        "email": "Email Address",
        "emailPlaceholder": "your@email.com",
        "subject": "Subject",
        "subjectPlaceholder": "How can we help?",
        "message": "Message",
        "messagePlaceholder": "Tell us about your inquiry...",
        "submit": "Send Message"
      },
      "quote": {
        "title": "Request a Quote",
        "name": "Full Name",
        "namePlaceholder": "Your full name",
        "email": "Email Address",
        "emailPlaceholder": "your@email.com",
        "company": "Company Name",
        "companyPlaceholder": "Your company (optional)",
        "product": "Product / Service",
        "productPlaceholder": "What product or service do you need?",
        "quantity": "Quantity",
        "quantityPlaceholder": "e.g., 1000 units, 20 tons",
        "destinationCountry": "Destination Country",
        "destinationCountryPlaceholder": "Where should it be delivered?",
        "details": "Additional Details",
        "detailsPlaceholder": "Any specific requirements or notes...",
        "submit": "Request Quote"
      },
      "sourcing": {
        "title": "Submit Sourcing Request",
        "name": "Full Name",
        "namePlaceholder": "Your full name",
        "email": "Email Address",
        "emailPlaceholder": "your@email.com",
        "company": "Company Name",
        "companyPlaceholder": "Your company (optional)",
        "product": "Product Name",
        "productPlaceholder": "What product are you looking for?",
        "specifications": "Specifications",
        "specificationsPlaceholder": "Describe the product specifications, quality requirements, certifications needed...",
        "quantity": "Quantity",
        "quantityPlaceholder": "e.g., 500 kg, 10,000 pieces",
        "targetCountry": "Target Country",
        "targetCountryPlaceholder": "Delivery destination",
        "submit": "Submit Request"
      },
      "callback": {
        "title": "Request a Callback",
        "name": "Full Name",
        "namePlaceholder": "Your full name",
        "phone": "Phone Number",
        "phonePlaceholder": "+90 5XX XXX XX XX",
        "preferredTime": "Preferred Time",
        "preferredTimePlaceholder": "e.g., Weekdays 10:00-12:00",
        "subject": "Brief Subject",
        "subjectPlaceholder": "What would you like to discuss?",
        "submit": "Request Callback"
      }
    }
    ```

    2. **`Validation` namespace** -- field-level validation error messages:

    ```json
    "Validation": {
      "name": {
        "too_small": "Name must be at least 2 characters",
        "too_big": "Name must be less than 100 characters"
      },
      "email": {
        "invalid_string": "Please enter a valid email address"
      },
      "subject": {
        "too_small": "Subject must be at least 3 characters",
        "too_big": "Subject must be less than 200 characters"
      },
      "message": {
        "too_small": "Message must be at least 10 characters",
        "too_big": "Message must be less than 5000 characters"
      },
      "product": {
        "too_small": "Product name must be at least 3 characters",
        "too_big": "Product name must be less than 300 characters"
      },
      "quantity": {
        "too_small": "Quantity is required"
      },
      "destinationCountry": {
        "too_small": "Destination country is required"
      },
      "targetCountry": {
        "too_small": "Target country is required"
      },
      "specifications": {
        "too_small": "Specifications must be at least 10 characters",
        "too_big": "Specifications must be less than 5000 characters"
      },
      "phone": {
        "invalid_string": "Please enter a valid phone number"
      },
      "preferredTime": {
        "too_small": "Please specify a preferred time"
      }
    }
    ```

    3. **Page-level translations** for the 3 new pages and updated contact page:

    ```json
    "QuotePage": {
      "title": "Get a Quote",
      "subtitle": "Tell us about your requirements and we will provide a competitive quote within 24 hours.",
      "badge": "Quote Request"
    },
    "SourcingPage": {
      "title": "Sourcing Request",
      "subtitle": "Looking for specific products from Turkey? Share your requirements and our sourcing team will find the best suppliers.",
      "badge": "Product Sourcing"
    },
    "CallbackPage": {
      "title": "Request a Callback",
      "subtitle": "Leave your number and our trade experts will call you at your preferred time.",
      "badge": "Callback",
      "benefits": {
        "fast": "Quick response within the hour",
        "noObligation": "No obligation consultation",
        "expert": "Expert trade advisors"
      }
    }
    ```

    Add to `ContactPage`: `"responseTime": "We typically respond within 24 hours"`

    Add to `Common`: `"whatsappChat": "Chat on WhatsApp"`, `"optional": "(optional)"`

    **TRANSLATIONS**: Provide proper, natural-sounding translations for each locale:
    - **TR (Turkish)**: Use professional business Turkish. This is the primary locale.
    - **EN (English)**: As shown in the examples above.
    - **FR (French)**: Professional business French. Use formal "vous" form.
    - **RU (Russian)**: Professional business Russian with proper business terminology. Use formal register. Per existing pattern from Phase 2, give Russian translations extra detail for CIS audience.

    **CRITICAL**: Read each JSON file completely before editing. Preserve ALL existing keys. Only ADD the new namespaces. Ensure valid JSON (no trailing commas, proper quoting).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['tr','en','fr','ru'].forEach(l=>{const j=JSON.parse(fs.readFileSync('src/messages/'+l+'.json','utf8')); const has=['Forms','Validation','QuotePage','SourcingPage','CallbackPage'].every(k=>j[k]); console.log(l+': '+(has?'OK':'MISSING KEYS'))})"</automated>
  </verify>
  <done>
    All 4 locale files contain Forms, Validation, QuotePage, SourcingPage, CallbackPage namespaces. All form labels, placeholders, validation errors, success/error messages are properly translated in Turkish, English, French, and Russian. Existing translations preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update navigation links and create server action integration tests</name>
  <files>
    src/components/layout/header.tsx,
    src/components/layout/footer.tsx,
    src/__tests__/form-actions.test.ts
  </files>
  <action>
    **Step 1: Update header navigation** (`src/components/layout/header.tsx`)
    Read the existing header. The contact link already exists in navigation. Add a "Get a Quote" CTA button or ensure the existing CTA button in the header links to the quote page (/quote). If there is a "Get in Touch" CTA button, consider changing it to link to /contact (which now has a real form). The other form pages (sourcing, callback) should be discoverable but not clutter the main nav -- they can be accessed from CTA sections on relevant pages. Do NOT add sourcing/callback to the main navigation bar.

    Check how the header currently handles navigation items and CTA. If there's already a CTA button linking to contact, keep it. The main goal: the header CTA button should link to /contact (or /quote if "Get a Quote" is more appropriate for the CTA button).

    **Step 2: Update footer** (`src/components/layout/footer.tsx`)
    Read the existing footer. Add form page links to the appropriate footer column. Under the "Services" or a new "Quick Links" section, add:
    - Contact (already exists likely)
    - Get a Quote (/quote)
    - Sourcing Request (/sourcing)
    - Request Callback (/callback)

    Use `useTranslations` for the link labels. Add translation keys to the footer namespace if needed (add `"quote": "Get a Quote"`, `"sourcing": "Sourcing Request"`, `"callback": "Request Callback"` under `Footer.services` or a new `Footer.forms` section).

    Wait -- the footer translations will need to be included in Step 1's translation additions. Add these keys to the Footer namespace in all 4 locale files during Task 1 as well:

    ```json
    "Footer": {
      "forms": {
        "title": "Get in Touch",
        "contact": "Contact Form",
        "quote": "Request a Quote",
        "sourcing": "Sourcing Request",
        "callback": "Request Callback"
      }
    }
    ```

    (Merge into existing Footer object, do not replace.)

    **Step 3: Create server action integration tests** (`src/__tests__/form-actions.test.ts`)

    Test the submitContactForm server action with mocked dependencies:
    - Mock `next-intl/server` (getTranslations returns a function that returns the key)
    - Mock `next/headers` (headers returns a Map with x-forwarded-for)
    - Mock `@/lib/email/resend` (sendNotificationEmail is a vi.fn())
    - Mock `@/lib/recaptcha` (verifyRecaptcha returns true)
    - Mock `@/lib/rate-limit` (rateLimit returns { success: true })

    Tests:
    1. **Valid submission**: Create FormData with valid contact data + recaptchaToken. Call submitContactForm with initial state and formData. Assert result.success === true. Assert sendNotificationEmail was called with ('contact', matching data).
    2. **Validation failure**: Create FormData with empty name. Assert result.success === false. Assert result.errors contains 'name'. Assert sendNotificationEmail was NOT called.
    3. **Rate limit exceeded**: Mock rateLimit to return { success: false }. Assert result.success === false. Assert result.message contains 'rateLimited'.
    4. **Honeypot triggered**: Create FormData with website='spam'. Assert result.success === true (fake success). Assert sendNotificationEmail was NOT called.
    5. **reCAPTCHA failure**: Mock verifyRecaptcha to return false. Assert result.success === false. Assert result.message contains 'recaptchaFailed'.
    6. **Email send failure**: Mock sendNotificationEmail to throw. Assert result.success === false. Assert result.message contains 'sendFailed'.

    Reset mocks between tests with beforeEach.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/form-actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    Header CTA links to contact or quote page. Footer includes links to all 4 form pages. Server action tests verify the full pipeline: valid submission sends email, validation errors are returned, rate limiting blocks, honeypot silently succeeds, reCAPTCHA failure rejects, email errors are caught. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/form-actions.test.ts --reporter=verbose` all 6 tests pass
- `node -e "['tr','en','fr','ru'].forEach(l=>{const j=JSON.parse(require('fs').readFileSync('src/messages/'+l+'.json','utf8'));console.log(l+':',!!j.Forms&&!!j.Validation)})"` prints true for all 4
- `npm run build` succeeds (all pages generate across 4 locales)
- `npm test` full suite passes
</verification>

<success_criteria>
1. All form text (labels, placeholders, errors, success messages) properly translated in TR, EN, FR, RU
2. Form pages discoverable via header CTA and footer links
3. Server action tests verify: valid submit sends email, validation errors returned, rate limit blocks, honeypot silently passes, reCAPTCHA rejects, email error caught
4. Full test suite passes
5. Build succeeds with all pages in all 4 locales
</success_criteria>

<output>
After completion, create `.planning/phases/03-forms-and-lead-capture/03-03-SUMMARY.md`
</output>
