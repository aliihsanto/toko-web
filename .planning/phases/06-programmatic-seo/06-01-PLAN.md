---
phase: 06-programmatic-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/pseo/types.ts
  - src/data/pseo/products.ts
  - src/data/pseo/countries.ts
  - src/data/pseo/faqs.ts
  - src/data/pseo/customs.ts
  - src/lib/pseo/utils.ts
  - src/__tests__/pseo-data.test.ts
autonomous: true
requirements: [PSEO-01, PSEO-02, PSEO-03, PSEO-04, PSEO-05]

must_haves:
  truths:
    - "TypeScript interfaces define the shape of all 4 PSEO data types with locale-keyed content"
    - "Product data file contains entries for all 8 sectors with ~4 product slugs each (~30 entries), each with 500+ words per locale"
    - "Country data file contains ~20 trade partner countries with unique content per locale"
    - "FAQ data file contains ~12 FAQ topic pages (8 sector + 4 general) with 8-10 Q&A pairs each"
    - "Customs data file contains ~15 customs/regulations pages with procedures, documents, HS codes"
    - "Utility helpers resolve locale content and look up pages by slug"
    - "Automated tests validate data completeness, locale coverage, word count minimums, and slug uniqueness"
  artifacts:
    - path: "src/data/pseo/types.ts"
      provides: "TypeScript interfaces for LocaleContent, ProductPageData, CountryPageData, FAQPageData, CustomsPageData"
      exports: ["LocaleContent", "ProductPageData", "CountryPageData", "FAQPageData", "CustomsPageData"]
    - path: "src/data/pseo/products.ts"
      provides: "Array of ~30 product import page data entries"
      exports: ["productPages"]
    - path: "src/data/pseo/countries.ts"
      provides: "Array of ~20 country trade guide data entries"
      exports: ["countryPages"]
    - path: "src/data/pseo/faqs.ts"
      provides: "Array of ~12 FAQ page data entries with 8-10 Q&A per page"
      exports: ["faqPages"]
    - path: "src/data/pseo/customs.ts"
      provides: "Array of ~15 customs/regulations page data entries"
      exports: ["customsPages"]
    - path: "src/lib/pseo/utils.ts"
      provides: "Content accessor helpers"
      exports: ["t", "getProductPage", "getCountryPage", "getFAQPage", "getCustomsPage", "getAllProductSlugs", "getAllCountrySlugs", "getAllFAQSlugs", "getAllCustomsSlugs"]
    - path: "src/__tests__/pseo-data.test.ts"
      provides: "Data validation tests for all PSEO data files"
  key_links:
    - from: "src/data/pseo/products.ts"
      to: "src/data/pseo/types.ts"
      via: "imports ProductPageData type"
      pattern: "import.*ProductPageData.*from.*types"
    - from: "src/lib/pseo/utils.ts"
      to: "src/data/pseo/*.ts"
      via: "imports all page data arrays"
      pattern: "import.*productPages|countryPages|faqPages|customsPages"
---

<objective>
Create the complete data infrastructure for programmatic SEO: TypeScript interfaces, JSON-structured data files for all 4 page types (products, countries, FAQs, customs), utility helpers, and validation tests.

Purpose: Establish the single source of truth for all programmatic page content. Content lives in separate data files (NOT translation files) to avoid bloating next-intl message bundles. Every entry must have substantive, unique content (500+ words per locale) to avoid Google thin content penalties.

Output: 6 data/utility files + 1 test file. All downstream plans (06-02 template pages, 06-03 SEO integration) import from these files.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-programmatic-seo/06-RESEARCH.md

@src/data/sectors.ts
@src/data/services.ts
@src/i18n/routing.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/data/sectors.ts:
```typescript
export interface SectorItem {
  slug: string;
  iconName: string;
  image: string;
  productKeys: string[];
}
export const sectors: SectorItem[] = [
  { slug: 'food', productKeys: ['grains', 'dairyProducts', 'cannedGoods', 'spices'] },
  { slug: 'textile', productKeys: ['fabrics', 'readyToWear', 'homeTextiles', 'industrialTextiles'] },
  { slug: 'machinery', productKeys: ['industrialMachinery', 'agriculturalEquipment', 'spareParts', 'automation'] },
  { slug: 'chemicals', productKeys: ['industrialChemicals', 'petrochemicals', 'pharmaceuticals', 'fertilizers'] },
  { slug: 'construction', productKeys: ['buildingMaterials', 'steel', 'cement', 'plumbing'] },
  { slug: 'raw-materials', productKeys: ['metals', 'minerals', 'timber', 'polymers'] },
  { slug: 'electronics', productKeys: ['components', 'consumerElectronics', 'cables', 'lighting'] },
  { slug: 'automotive', productKeys: ['autoParts', 'tires', 'accessories', 'commercialVehicles'] },
];
```

From src/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['tr', 'en', 'fr', 'ru'],
  defaultLocale: 'tr',
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript interfaces, utility helpers, and validation test scaffold</name>
  <files>src/data/pseo/types.ts, src/lib/pseo/utils.ts, src/__tests__/pseo-data.test.ts</files>
  <action>
**1. Create `src/data/pseo/types.ts`** with these interfaces:

```typescript
export interface LocaleContent {
  tr: string;
  en: string;
  fr: string;
  ru: string;
}
```

- `ProductPageData`: slug, sectorSlug (links to sectors.ts), image (Unsplash URL), meta (title, description as LocaleContent), content object with: title, subtitle (LocaleContent), overview (LocaleContent[] -- array of 3-4 paragraphs), keyFacts ({label, value} as LocaleContent, array of 5-6), tradeAdvantages ({title, description} as LocaleContent, array of 4), process ({step, title, description} as LocaleContent, array of 4-5), qualityStandards (LocaleContent -- paragraph), cta ({title, description, buttonText} as LocaleContent).

- `CountryPageData`: slug, flag (emoji), image, meta (title, description), content object with: title, subtitle (LocaleContent), overview (LocaleContent[] -- 3-4 paragraphs), tradeStats ({label, value} as LocaleContent, array of 6), keySectors ({title, description} as LocaleContent, array of 4-5), agreements (LocaleContent -- paragraph about FTAs/customs unions), logistics ({title, description} as LocaleContent, array of 3-4 covering routes/ports/transit times), cta.

- `FAQPageData`: slug, topic (LocaleContent -- the topic label), meta (title, description), content object with: title, subtitle (LocaleContent), intro (LocaleContent -- 1 paragraph), questions ({question, answer} as LocaleContent, array of 8-10), relatedLinks ({label, href} array -- internal links to related PSEO pages).

- `CustomsPageData`: slug, image, meta (title, description), content object with: title, subtitle (LocaleContent), overview (LocaleContent[] -- 2-3 paragraphs), procedures ({title, description} as LocaleContent, array of 4-5), requiredDocuments ({name, description} as LocaleContent, array of 5-7), hsCodes ({code, description, dutyRate} with code as string and others as LocaleContent, array of 4-6), restrictions (LocaleContent -- paragraph), tokoSupport (LocaleContent -- paragraph), cta.

**2. Create `src/lib/pseo/utils.ts`** with:

- `t(content: LocaleContent, locale: string): string` -- resolves locale string, falls back to `en` if locale key missing.
- `getProductPage(slug: string): ProductPageData | undefined`
- `getCountryPage(slug: string): CountryPageData | undefined`
- `getFAQPage(slug: string): FAQPageData | undefined`
- `getCustomsPage(slug: string): CustomsPageData | undefined`
- `getAllProductSlugs(): string[]`
- `getAllCountrySlugs(): string[]`
- `getAllFAQSlugs(): string[]`
- `getAllCustomsSlugs(): string[]`
- `countWords(text: string): number` -- splits on whitespace, returns count. Used by tests.

Import the data arrays from their respective files (will be created in Task 2). For now, import from the paths that Task 2 will create. The file won't compile until Task 2 creates the data files, but that's fine -- both tasks are in the same plan and Task 2 runs after.

**3. Create `src/__tests__/pseo-data.test.ts`** with validation tests:

- **Locale completeness**: Every LocaleContent field in every data entry has non-empty strings for all 4 locales (tr, en, fr, ru).
- **Slug uniqueness**: No duplicate slugs within any data file.
- **Slug format**: All slugs are lowercase kebab-case matching `/^[a-z0-9]+(-[a-z0-9]+)*$/`.
- **Word count minimum (PSEO-05)**: For each page in each locale, concatenate all content text fields and verify total >= 500 words. Use the `countWords` utility.
- **Product data**: Each entry has a valid sectorSlug that exists in sectors.ts.
- **FAQ data**: Each entry has at least 8 questions.
- **Data array lengths**: productPages.length >= 25, countryPages.length >= 15, faqPages.length >= 10, customsPages.length >= 12.
- **Meta fields**: Every entry has non-empty meta.title and meta.description for all locales.

Use vitest. Group tests by data type using `describe` blocks: "Product Pages", "Country Pages", "FAQ Pages", "Customs Pages", "Cross-cutting".
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pseo-data.test.ts --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Types file exports all 5 interfaces, utils file exports all accessor functions, test file exists (will fail until Task 2 provides data -- that is expected)</done>
</task>

<task type="auto">
  <name>Task 2: Populate all 4 PSEO data files with substantive multilingual content</name>
  <files>src/data/pseo/products.ts, src/data/pseo/countries.ts, src/data/pseo/faqs.ts, src/data/pseo/customs.ts</files>
  <action>
Create all 4 data files with complete, substantive content. This is the bulk of the work -- each page entry needs 500+ words of UNIQUE content per locale. Do NOT use mad-libs templates where only a product/country name changes. Each entry must have genuinely different paragraphs, facts, and statistics.

**CRITICAL CONTENT GUIDELINES:**
- All content must be pre-written, not generated at runtime
- Turkish content should use proper Turkish characters (ş, ç, ğ, ı, ö, ü)
- Russian content must use proper Cyrillic (not Latin transliteration)
- French content must use proper accents (é, è, ê, à, ç, etc.)
- Trade statistics should reference realistic data (Turkey is a real major exporter)
- Each page's content sections must be genuinely unique -- different facts, different trade advantages, different processes
- Images: use Unsplash URLs with relevant keywords (e.g., food, textile, machinery photos)

**1. `src/data/pseo/products.ts`** -- Export `productPages: ProductPageData[]`

Create entries for the 8 sectors, with 3-4 products each (aim for ~30 total). Derive slugs from the sector's productKeys in sectors.ts. Examples:

- food: `food-grains`, `food-dairy`, `food-canned-goods`, `food-spices`
- textile: `textile-fabrics`, `textile-ready-to-wear`, `textile-home-textiles`, `textile-industrial`
- machinery: `machinery-industrial`, `machinery-agricultural`, `machinery-spare-parts`, `machinery-automation`
- chemicals: `chemicals-industrial`, `chemicals-petrochemicals`, `chemicals-pharmaceuticals`, `chemicals-fertilizers`
- construction: `construction-building-materials`, `construction-steel`, `construction-cement`, `construction-plumbing`
- raw-materials: `raw-metals`, `raw-minerals`, `raw-timber`, `raw-polymers`
- electronics: `electronics-components`, `electronics-consumer`, `electronics-cables`, `electronics-lighting`
- automotive: `auto-parts`, `auto-tires`, `auto-accessories`, `auto-commercial-vehicles`

Each product page must include:
- meta.title in format "[Product] Import from Turkey | Toko Trading" (localized)
- meta.description: 150-160 char SEO description
- 3-4 overview paragraphs about Turkey's production and export of this specific product category
- 5-6 key facts (production volume, export value, key destinations, HS code ranges, certifications)
- 4 trade advantages specific to this product (not generic)
- 4-5 import process steps
- Quality standards paragraph
- CTA with product-specific messaging

**2. `src/data/pseo/countries.ts`** -- Export `countryPages: CountryPageData[]`

Create 20 country entries covering Turkey's major trade partners:
`germany`, `united-kingdom`, `france`, `italy`, `spain`, `netherlands`, `russia`, `united-states`, `china`, `iraq`, `saudi-arabia`, `uae`, `egypt`, `israel`, `romania`, `bulgaria`, `poland`, `japan`, `south-korea`, `brazil`

Each country page must include:
- Flag emoji for the country
- Overview paragraphs about bilateral trade relationship (Turkey-[Country])
- 6 trade statistics (bilateral trade volume, top exports, top imports, trade balance, growth rate, FTA status)
- 4-5 key traded sectors between the countries
- Agreements paragraph (free trade agreements, customs unions, bilateral treaties)
- 3-4 logistics items (shipping routes, transit times, major ports/border crossings)
- CTA

**3. `src/data/pseo/faqs.ts`** -- Export `faqPages: FAQPageData[]`

Create 12 FAQ pages:
- 8 sector-specific: `faq-food-import`, `faq-textile-import`, `faq-machinery-import`, `faq-chemicals-import`, `faq-construction-import`, `faq-raw-materials-import`, `faq-electronics-import`, `faq-automotive-import`
- 4 general: `faq-customs-procedures`, `faq-documentation`, `faq-shipping-logistics`, `faq-payment-terms`

Each FAQ page must include:
- Intro paragraph setting context
- 8-10 Q&A pairs with detailed 40-60 word answers (not one-liners)
- relatedLinks pointing to relevant `/trade/import/[slug]` or `/trade/customs/[slug]` pages

**4. `src/data/pseo/customs.ts`** -- Export `customsPages: CustomsPageData[]`

Create 15 customs/regulations pages:
- By region/country: `customs-eu`, `customs-russia-cis`, `customs-middle-east`, `customs-north-africa`, `customs-east-asia`, `customs-south-america`, `customs-north-america`, `customs-southeast-asia`
- By topic: `customs-food-safety`, `customs-textile-regulations`, `customs-chemicals-reach`, `customs-machinery-standards`, `customs-dangerous-goods`, `customs-organic-certification`, `customs-halal-certification`

Each customs page must include:
- 2-3 overview paragraphs about the regulatory landscape
- 4-5 customs procedures with descriptions
- 5-7 required documents with descriptions
- 4-6 HS code entries with description and duty rate info
- Restrictions paragraph
- Toko support paragraph
- CTA

**CONTENT VOLUME NOTE**: This task creates a large amount of content (~77 pages x 4 locales). Write substantive English content first, then create TR, FR, RU translations. Each locale version should be a proper translation -- not a word-for-word machine translation. Adapt terminology for the target audience (e.g., Russian business terminology for RU, formal French for FR).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/pseo-data.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 4 data files export their respective arrays. All validation tests pass: locale completeness, slug uniqueness, word count >= 500 per page per locale, correct array lengths. `npm run build` does not fail (data files are valid TypeScript).</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/pseo-data.test.ts --reporter=verbose` -- all data validation tests pass
2. `npx tsc --noEmit` -- no TypeScript errors in data files or utils
3. Spot-check: at least 3 product pages, 3 country pages, 2 FAQ pages, 2 customs pages have genuinely different content (not templated)
4. Word count: verify at least 5 random pages have 500+ words in English locale
</verification>

<success_criteria>
- src/data/pseo/types.ts exports all 5 interfaces (LocaleContent + 4 page data types)
- src/data/pseo/products.ts exports ~30 product page entries covering all 8 sectors
- src/data/pseo/countries.ts exports ~20 country trade guide entries
- src/data/pseo/faqs.ts exports ~12 FAQ page entries with 8-10 Q&A each
- src/data/pseo/customs.ts exports ~15 customs/regulations page entries
- src/lib/pseo/utils.ts exports t() helper and all accessor functions
- src/__tests__/pseo-data.test.ts passes with all validations green
- Every page entry has 500+ words of content per locale
- No duplicate slugs across any data file
- All 4 locales (tr, en, fr, ru) have non-empty content for every field
</success_criteria>

<output>
After completion, create `.planning/phases/06-programmatic-seo/06-01-SUMMARY.md`
</output>
