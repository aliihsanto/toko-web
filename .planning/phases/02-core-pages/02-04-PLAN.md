---
phase: 02-core-pages
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/references/page.tsx
  - src/app/[locale]/russia-transit/page.tsx
  - src/messages/tr.json
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/ru.json
autonomous: true
requirements:
  - PAGE-07
  - PAGE-08

must_haves:
  truths:
    - "References page shows industry categories, aggregate stats, and testimonial-style cards with flexible layout"
    - "Russia transit trade page presents dedicated content with Russian-language design priority and specialized CTAs"
    - "Russia transit page has distinct visual identity from other pages (Cyrillic-first hierarchy, unique imagery)"
  artifacts:
    - path: "src/app/[locale]/references/page.tsx"
      provides: "References page with partner sectors, stats, testimonials"
      min_lines: 100
    - path: "src/app/[locale]/russia-transit/page.tsx"
      provides: "Russia transit trade landing page"
      min_lines: 120
  key_links:
    - from: "src/app/[locale]/russia-transit/page.tsx"
      to: "/contact"
      via: "Specialized CTA buttons for Russian trade inquiries"
      pattern: "Link.*href.*contact"
    - from: "src/app/[locale]/references/page.tsx"
      to: "next-intl"
      via: "getTranslations for ReferencesPage namespace"
      pattern: "getTranslations.*ReferencesPage"
    - from: "src/app/[locale]/russia-transit/page.tsx"
      to: "next-intl"
      via: "getTranslations for RussiaTransitPage namespace"
      pattern: "getTranslations.*RussiaTransitPage"
---

<objective>
Build the references/partners page and the Russia transit trade landing page.

Purpose: The references page builds trust by showing Toko's industry reach and client satisfaction. The Russia transit trade page is a specialized landing page targeting Russian/CIS trade -- a key business differentiator.
Output: Complete references page, complete Russia transit trade landing page, translations for all 4 locales.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (shared infrastructure) -->
From src/components/common/page-hero.tsx:
```typescript
export function PageHero(props: { title: string; subtitle: string; backgroundImage?: string; badge?: string }): JSX.Element;
```

From src/components/common/cta-section.tsx:
```typescript
export function CTASection(props: { title: string; description: string; buttonText: string; buttonHref: string; note?: string }): JSX.Element;
```

From src/components/common/breadcrumb.tsx:
```typescript
export function Breadcrumb({ items }: { items: Array<{ label: string; href?: string }> }): JSX.Element;
```

<!-- Established patterns -->
- Page structure: PageHero > Breadcrumb > Content sections > CTA
- Color variety: alternate bg-white, bg-gray-50, bg-amber-50, bg-emerald-50, bg-[#1e3043]
- All text from translations via getTranslations
- ScrollReveal for section animations
- next/image for all images
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build references page with industry icons, stats, and testimonial cards</name>
  <files>src/app/[locale]/references/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Replace the placeholder references page with a complete layout. Per PROJECT.md, Toko's reference data is limited (many deals through intermediaries), so design for credibility without requiring named clients.

1. **PageHero**: Title "Our Track Record", subtitle about proven results in international trade, business/handshake background image. Badge: "Trusted Partner".

2. **Breadcrumb**: Home > References.

3. **Key Statistics section** (bg-white dark:bg-background):
   - Large centered stats row showing aggregate numbers:
     - "1000+" transactions completed
     - "50+" countries reached
     - "20+" years of experience
     - "8+" industry sectors
   - Stats displayed as large bold numbers with descriptive labels below
   - Amber accent on numbers
   - ScrollReveal with stagger

4. **Industries Served section** (bg-gray-50 dark:bg-card):
   - Section title: "Industries We Serve"
   - Grid of 8 industry cards (matching the 8 sectors) with:
     - Industry icon from lucide-react
     - Translated industry name
     - Brief description (1 line)
   - Hover effect on cards
   - These are NOT links to sector pages -- just visual representation of breadth

5. **Client Testimonials section** (bg-amber-50 dark:bg-amber-950/20):
   - Section title: "What Our Partners Say"
   - 3 testimonial cards in a grid:
     - Each card: quoted text, company description (anonymized: "Leading Textile Manufacturer, Germany"), star rating (5 stars)
     - Cards have subtle shadow, rounded corners, quote icon
   - Use realistic but anonymized testimonials appropriate for B2B trade
   - Keys: `ReferencesPage.testimonials.t1.quote`, `.company`, `.role` (through t3)

6. **Global Presence section** (bg-[#1e3043] dark:bg-[#14202e]):
   - "Our Global Footprint"
   - List of 5 regions with country counts: Europe (15+), Middle East (10+), Asia (12+), Africa (8+), Russia & CIS (5+)
   - Each region as a pill/tag with count
   - Optional: mention of key trade corridors (Turkey-Germany, Turkey-Russia, Turkey-UAE, etc.)

7. **CTA Section**: "Ready to join our partner network?" linking to /contact

8. **Translation updates** for all 4 locales under `ReferencesPage` namespace:
   - `hero.title`, `hero.subtitle`, `hero.badge`
   - `stats.*` (4 stat labels and values)
   - `industries.title`, `industries.[sector].title`, `industries.[sector].description` for 8 sectors
   - `testimonials.title`, `testimonials.t1.quote`, `.company`, `.role` (through t3)
   - `global.title`, `global.regions.*`
   - `cta.*`
   - Remove old `comingSoon` key
   - Testimonials should feel authentic and region-appropriate
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx tsc --noEmit && npx vitest run src/__tests__/references-page.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>References page has 5 sections: stats, industries grid, testimonials (amber background), global presence (dark navy), CTA. No named client data needed. All text translated in 4 locales. TypeScript passes. References page tests run (todos skipped, no failures).</done>
</task>

<task type="auto">
  <name>Task 2: Build Russia transit trade landing page with Russian-first design</name>
  <files>src/app/[locale]/russia-transit/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Create `src/app/[locale]/russia-transit/page.tsx` as a specialized landing page. This page must feel distinct from generic service pages -- it targets Russian/CIS trade specifically.

1. **PageHero** (enhanced for this page):
   - Larger hero (min-h-[50vh]) with dramatic background image: use a Bosphorus/Istanbul strait image or Turkey-Russia trade corridor imagery (cargo ship in strait, or similar)
   - Badge: "Turkey - Russia Trade Corridor" (translated)
   - Title emphasizing the Turkey-Russia trade bridge
   - Subtitle about specialized transit trade expertise
   - TWO CTA buttons: "Request Quote" (amber) and "Call Us" (outline) -- both linking to /contact

2. **Breadcrumb**: Home > Russia Transit Trade.

3. **What Is Transit Trade section** (bg-white dark:bg-background):
   - Two-column layout explaining transit trade concept
   - Left: 2-3 paragraphs explaining how Turkey serves as a trade bridge to Russia
   - Right: Relevant trade corridor image
   - Key points: geographical advantage, customs expertise, logistics network

4. **Advantages section** (bg-gray-50 dark:bg-card):
   - "Why Transit Through Turkey?" section
   - 4-6 advantage cards in a grid:
     - Strategic Location (Turkey bridges Europe and Asia)
     - Customs Expertise (Turkish customs procedures for re-export)
     - Logistics Network (warehousing, freight, ports)
     - CIS Market Knowledge (understanding of Russian/CIS regulations)
     - Cost Efficiency (competitive routing vs direct import)
     - Compliance Support (sanctions compliance, documentation)
   - Each card: icon, title, description

5. **Trade Routes section** (bg-emerald-50 dark:bg-emerald-950/20):
   - "Key Trade Corridors" showing main routes:
     - Turkey -> Russia (Moscow, St. Petersburg)
     - Turkey -> Kazakhstan
     - Turkey -> Azerbaijan
     - Turkey -> Georgia
     - Turkey -> Uzbekistan
   - Displayed as route cards or a visual flow
   - Each route: origin flag emoji or marker, destination, typical goods

6. **Process section** (bg-[#1e3043] dark:bg-[#14202e]):
   - "How It Works" with 5 numbered steps specific to transit trade
   - Steps: Inquiry -> Route Planning -> Documentation -> Shipment -> Delivery Confirmation
   - Light text on dark background

7. **FAQ section** (bg-white dark:bg-background):
   - 4-5 common questions about transit trade:
     - "What is transit trade?"
     - "What products can be transited through Turkey?"
     - "How long does transit typically take?"
     - "What documentation is required?"
     - "How does compliance work?"
   - Simple stacked Q&A cards (bold question, description answer below) -- no accordion component needed
   - Keys: `RussiaTransitPage.faq.q1.question`, `.answer` (through q5)

8. **CTA Section**: Enhanced CTA with WhatsApp mention -- "Contact our Russia desk directly" with both email CTA and note about WhatsApp availability

9. **Translation updates** for all 4 locales under `RussiaTransitPage` namespace:
   - Full translation set for all sections
   - Russian (ru) translations should be given extra attention -- this is the primary target audience
   - Russian content should be more detailed and use professional business Russian
   - Include relevant Russian trade terminology where appropriate
   - Turkish content should also be strong (Toko is Turkey-based)

10. **Navigation**: Do NOT add to main header navigation (7 items already, adding more would crowd it). Russia transit page is accessible via:
    - Direct URL /russia-transit
    - The transit-trade service detail page can cross-link to this page
    - Homepage transit trade service card can mention it
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx tsc --noEmit && npx vitest run src/__tests__/russia-transit.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Russia transit trade page has 7 sections: enhanced hero, what-is-transit, advantages grid, trade routes, process steps (dark), FAQ, and CTA. Page feels distinct from generic pages. Russian translations are detailed and professional. Route accessible at /russia-transit in all locales. TypeScript passes. Russia transit tests run (todos skipped, no failures).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no TypeScript errors
2. `npm run build` -- all pages build successfully including new routes
3. `npx vitest run src/__tests__/references-page.test.tsx src/__tests__/russia-transit.test.tsx --reporter=verbose` -- tests run without failures
4. References page has flexible layout without named clients
5. Russia transit page has distinct visual identity
6. No hardcoded text on any page -- all from translations
7. Color variety across pages (amber, emerald, gray, navy -- not just blue/white)
</verification>

<success_criteria>
- References page complete with flexible layout (no named clients needed)
- Russia transit trade page has Russian-priority design and specialized content
- All translations present in TR, EN, FR, RU
- Build passes, tests pass
- No regressions in existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-04-SUMMARY.md`
</output>
