---
phase: 02-core-pages
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/homepage.test.tsx
  - src/__tests__/about-page.test.tsx
  - src/__tests__/services-page.test.tsx
  - src/__tests__/service-detail.test.tsx
  - src/__tests__/sectors-page.test.tsx
  - src/__tests__/sector-detail.test.tsx
  - src/__tests__/references-page.test.tsx
  - src/__tests__/russia-transit.test.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
  - PAGE-07
  - PAGE-08

must_haves:
  truths:
    - "All 8 Phase 2 test stub files exist and are runnable by vitest"
    - "Each test file contains at least one pending/skipped test describing the expected page behavior"
    - "Running npx vitest run on the new test files produces 0 failures (skipped tests count as pass)"
  artifacts:
    - path: "src/__tests__/homepage.test.tsx"
      provides: "Test stubs for PAGE-01 homepage enhancements"
    - path: "src/__tests__/about-page.test.tsx"
      provides: "Test stubs for PAGE-02 about page"
    - path: "src/__tests__/services-page.test.tsx"
      provides: "Test stubs for PAGE-03 services overview"
    - path: "src/__tests__/service-detail.test.tsx"
      provides: "Test stubs for PAGE-04 service detail pages"
    - path: "src/__tests__/sectors-page.test.tsx"
      provides: "Test stubs for PAGE-05 sectors overview"
    - path: "src/__tests__/sector-detail.test.tsx"
      provides: "Test stubs for PAGE-06 sector detail pages"
    - path: "src/__tests__/references-page.test.tsx"
      provides: "Test stubs for PAGE-07 references page"
    - path: "src/__tests__/russia-transit.test.tsx"
      provides: "Test stubs for PAGE-08 Russia transit trade page"
  key_links: []
---

<objective>
Create all 8 Phase 2 test stub files so that vitest can validate Phase 2 work as plans execute.

Purpose: Without these test files, `npx vitest run` only exercises Phase 1 tests (49 tests) and Phase 2 verify commands pass vacuously. These stubs establish the Nyquist sampling contract -- each plan's verify command will run its specific test file and get real feedback.
Output: 8 new test files in src/__tests__/ with skipped tests describing expected behaviors, ready to be filled in by each plan's executor.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-RESEARCH.md

<interfaces>
<!-- Existing Phase 1 test mocking patterns from src/__tests__/header.test.tsx -->
```typescript
// Mock next-intl
vi.mock('next-intl', () => ({
  useTranslations: (namespace: string) => {
    const translations: Record<string, Record<string, string>> = { /* ... */ };
    return (key: string) => translations[namespace]?.[key] ?? key;
  },
  useLocale: () => 'en',
}));

// Mock i18n navigation
vi.mock('@/i18n/navigation', () => ({
  Link: ({ children, href, ...props }: any) => <a href={href} {...props}>{children}</a>,
  usePathname: () => '/',
  useRouter: () => ({ replace: vi.fn(), push: vi.fn() }),
}));
```

<!-- Phase 2 pages are server components using next-intl/server -->
<!-- Server component tests need to mock next-intl/server instead -->
```typescript
// Mock pattern for server components (Phase 2 pages)
vi.mock('next-intl/server', () => ({
  getTranslations: vi.fn(() => (key: string) => key),
  setRequestLocale: vi.fn(),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for homepage, about, services overview, and service detail</name>
  <files>src/__tests__/homepage.test.tsx, src/__tests__/about-page.test.tsx, src/__tests__/services-page.test.tsx, src/__tests__/service-detail.test.tsx</files>
  <action>
Create 4 test stub files following the Phase 1 mocking patterns. Each file should:
- Import `describe, it, expect, vi` from vitest
- Set up appropriate mocks for next-intl/server (getTranslations, setRequestLocale), next/navigation, @/i18n/navigation
- Use `it.todo()` for tests that describe expected behavior (these pass as skipped in vitest)
- Include 1 real test that imports the page component and verifies it exists (not undefined)

**src/__tests__/homepage.test.tsx** (PAGE-01):
```typescript
import { describe, it, expect, vi } from 'vitest';

// Mocks for server components
vi.mock('next-intl/server', () => ({
  getTranslations: vi.fn(() => (key: string) => key),
  setRequestLocale: vi.fn(),
}));

vi.mock('@/i18n/navigation', () => ({
  Link: ({ children, href, ...props }: any) => <a href={href} {...props}>{children}</a>,
}));

vi.mock('next/image', () => ({
  default: (props: any) => <img {...props} />,
}));

vi.mock('@/components/common/scroll-reveal', () => ({
  ScrollReveal: ({ children }: any) => <div>{children}</div>,
}));

describe('Homepage (PAGE-01)', () => {
  it.todo('renders services section with cards linking to /services/[slug]');
  it.todo('renders trust signals section with stats');
  it.todo('renders sectors preview section with amber background');
  it.todo('renders CTA section');
  it.todo('all visible text uses translation keys (no hardcoded strings)');
});
```

**src/__tests__/about-page.test.tsx** (PAGE-02):
```typescript
describe('About Page (PAGE-02)', () => {
  it.todo('renders PageHero with translated title and subtitle');
  it.todo('renders company overview section with paragraphs');
  it.todo('renders vision and mission cards');
  it.todo('renders company values section with amber background');
  it.todo('renders stats section with dark navy background');
  it.todo('renders CTA section linking to /contact');
  it.todo('renders breadcrumb with Home > About Us');
});
```

**src/__tests__/services-page.test.tsx** (PAGE-03):
```typescript
describe('Services Overview (PAGE-03)', () => {
  it.todo('renders PageHero with translated title');
  it.todo('renders all 4 service cards with detail links');
  it.todo('renders "Why Choose Our Services" section');
  it.todo('renders CTA section');
  it.todo('service cards link to /services/[slug]');
});
```

**src/__tests__/service-detail.test.tsx** (PAGE-04):
```typescript
describe('Service Detail Pages (PAGE-04)', () => {
  it.todo('exports generateStaticParams returning 4 service slugs');
  it.todo('exports dynamicParams = false');
  it.todo('renders PageHero with service-specific title');
  it.todo('renders overview section with paragraphs');
  it.todo('renders features grid with 5 features');
  it.todo('renders process steps section');
  it.todo('renders CTA section linking to /contact');
  it.todo('renders breadcrumb with Home > Services > [Service Name]');
});
```

Use the same mock setup block (next-intl/server, next/image, @/i18n/navigation, scroll-reveal) for all 4 files. Keep the files concise -- stubs only, no real render tests yet.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx vitest run src/__tests__/homepage.test.tsx src/__tests__/about-page.test.tsx src/__tests__/services-page.test.tsx src/__tests__/service-detail.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 test stub files exist. Vitest runs them successfully (all .todo tests show as skipped/pending, 0 failures).</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for sectors overview, sector detail, references, and Russia transit</name>
  <files>src/__tests__/sectors-page.test.tsx, src/__tests__/sector-detail.test.tsx, src/__tests__/references-page.test.tsx, src/__tests__/russia-transit.test.tsx</files>
  <action>
Create 4 more test stub files with the same mocking pattern as Task 1.

**src/__tests__/sectors-page.test.tsx** (PAGE-05):
```typescript
describe('Sectors Overview (PAGE-05)', () => {
  it.todo('renders PageHero with translated title');
  it.todo('renders all 8 sector image cards');
  it.todo('sector cards link to /sectors/[slug]');
  it.todo('renders industry stats section with emerald background');
  it.todo('renders CTA section');
});
```

**src/__tests__/sector-detail.test.tsx** (PAGE-06):
```typescript
describe('Sector Detail Pages (PAGE-06)', () => {
  it.todo('exports generateStaticParams returning 8 sector slugs');
  it.todo('exports dynamicParams = false');
  it.todo('renders PageHero with sector-specific title');
  it.todo('renders industry overview section');
  it.todo('renders products and capabilities grid');
  it.todo('renders sourcing advantages section');
  it.todo('renders CTA section linking to /contact');
  it.todo('renders breadcrumb with Home > Sectors > [Sector Name]');
});
```

**src/__tests__/references-page.test.tsx** (PAGE-07):
```typescript
describe('References Page (PAGE-07)', () => {
  it.todo('renders PageHero with translated title');
  it.todo('renders key statistics section');
  it.todo('renders industries served grid with 8 sectors');
  it.todo('renders client testimonials section with amber background');
  it.todo('renders global presence section with dark navy background');
  it.todo('renders CTA section linking to /contact');
  it.todo('renders breadcrumb with Home > References');
});
```

**src/__tests__/russia-transit.test.tsx** (PAGE-08):
```typescript
describe('Russia Transit Trade Page (PAGE-08)', () => {
  it.todo('renders enhanced PageHero with two CTA buttons');
  it.todo('renders "What Is Transit Trade" section');
  it.todo('renders advantages grid with 4-6 cards');
  it.todo('renders trade routes section with emerald background');
  it.todo('renders process steps section with dark navy background');
  it.todo('renders FAQ section with 4-5 questions');
  it.todo('renders specialized CTA section');
  it.todo('renders breadcrumb with Home > Russia Transit Trade');
});
```

Same mock setup block as Task 1 for all 4 files.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx vitest run src/__tests__/sectors-page.test.tsx src/__tests__/sector-detail.test.tsx src/__tests__/references-page.test.tsx src/__tests__/russia-transit.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 test stub files exist. Vitest runs them successfully (all .todo tests show as skipped/pending, 0 failures). All 8 Phase 2 test files now exist.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/homepage.test.tsx src/__tests__/about-page.test.tsx src/__tests__/services-page.test.tsx src/__tests__/service-detail.test.tsx src/__tests__/sectors-page.test.tsx src/__tests__/sector-detail.test.tsx src/__tests__/references-page.test.tsx src/__tests__/russia-transit.test.tsx --reporter=verbose` -- all 8 files run, 0 failures
2. All 8 files exist in src/__tests__/
3. Phase 1 tests still pass: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- 8 test stub files created matching VALIDATION.md Wave 0 requirements
- Each file uses consistent mocking patterns from Phase 1
- Vitest runs all 8 files with 0 failures (todo tests are skipped)
- Phase 1 tests (49 tests) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-00-SUMMARY.md`
</output>
