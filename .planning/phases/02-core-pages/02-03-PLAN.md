---
phase: 02-core-pages
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/sectors/page.tsx
  - src/app/[locale]/sectors/[slug]/page.tsx
  - src/messages/tr.json
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/ru.json
autonomous: true
requirements:
  - PAGE-05
  - PAGE-06

must_haves:
  truths:
    - "Sectors overview page shows all 8 industries served with images and links to detail pages"
    - "Each sector has its own detail page at /sectors/[slug] with industry-specific sourcing capabilities"
    - "Unknown sector slugs return 404 (dynamicParams = false)"
    - "Sector detail pages show hero, industry overview, products/capabilities, sourcing process, and CTA"
  artifacts:
    - path: "src/app/[locale]/sectors/page.tsx"
      provides: "Sectors overview with all 8 sectors and detail links"
      min_lines: 80
    - path: "src/app/[locale]/sectors/[slug]/page.tsx"
      provides: "Sector detail page with generateStaticParams"
      min_lines: 100
      contains: "generateStaticParams"
      exports: ["generateStaticParams", "generateMetadata"]
  key_links:
    - from: "src/app/[locale]/sectors/page.tsx"
      to: "/sectors/[slug]"
      via: "Link on each sector card"
      pattern: "Link.*href.*sectors/"
    - from: "src/app/[locale]/sectors/[slug]/page.tsx"
      to: "src/data/sectors.ts"
      via: "Import getSectorBySlug for data lookup"
      pattern: "import.*getSectorBySlug.*from.*@/data/sectors"
    - from: "src/app/[locale]/sectors/[slug]/page.tsx"
      to: "next-intl"
      via: "getTranslations for SectorDetail namespace"
      pattern: "getTranslations.*SectorDetail"
---

<objective>
Enhance the sectors overview page to show all 8 industries and create dynamic sector detail pages for each industry with sourcing capabilities.

Purpose: Toko serves multiple industries. The sectors pages help visitors from specific industries find relevant sourcing capabilities and build confidence that Toko understands their domain. Each sector detail page is an SEO landing page for industry-specific queries.
Output: Enhanced sectors overview with 8 sectors, 8 sector detail pages (statically generated), translations for all 4 locales.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (shared infrastructure) -->
From src/data/sectors.ts:
```typescript
export interface SectorItem {
  slug: string;
  iconName: string;
  image: string;
  productKeys: string[];
}
export const sectors: SectorItem[];
export function getSectorBySlug(slug: string): SectorItem | undefined;
export function getAllSectorSlugs(): string[];
```

From src/components/common/page-hero.tsx:
```typescript
export function PageHero(props: { title: string; subtitle: string; backgroundImage?: string; badge?: string }): JSX.Element;
```

From src/components/common/cta-section.tsx:
```typescript
export function CTASection(props: { title: string; description: string; buttonText: string; buttonHref: string; note?: string }): JSX.Element;
```

From src/components/common/breadcrumb.tsx:
```typescript
export function Breadcrumb({ items }: { items: Array<{ label: string; href?: string }> }): JSX.Element;
```

<!-- Established patterns from Plan 02-02 (services) -->
- Dynamic route pattern: generateStaticParams + dynamicParams = false
- Translation namespace pattern: SectorDetail.[slug].* for detail pages
- Page structure: PageHero > Breadcrumb > Content sections > CTA
- Icon mapping: local Record<string, ComponentType> for lucide icons
- Async params: params: Promise<{ locale: string; slug: string }>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance sectors overview page with all 8 sectors and detail links</name>
  <files>src/app/[locale]/sectors/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Rewrite `src/app/[locale]/sectors/page.tsx` to be a complete sectors overview:

1. **PageHero**: Translated title "Industry Sectors", subtitle about serving diverse industries, background image of industrial/trade scene. Badge: "8+ Industries".

2. **Breadcrumb**: Home > Sectors.

3. **Sectors Grid** (bg-white dark:bg-background):
   - Import `sectors` from `@/data/sectors`
   - Display all 8 sectors in a responsive grid: 4 columns on lg, 2 on md, 1 on mobile
   - Each sector card uses the image-overlay style (matching the existing sectors page pattern -- image with dark gradient overlay and text at bottom):
     - Full-height image card (h-80)
     - Gradient overlay from bottom
     - Sector icon from lucide (use iconMap like in services)
     - Translated sector name from `SectorsPage.sectors.[slug].title`
     - Short description from `SectorsPage.sectors.[slug].description`
     - Hover effect: image scales, overlay lightens, "Explore" text appears
     - Each card links to `/sectors/${sector.slug}`
   - Staggered ScrollReveal on cards

4. **Industry Stats section** (bg-emerald-50 dark:bg-emerald-950/20 -- green accent for nature/industry):
   - "Our Industry Reach" section
   - 4 stats: sectors served (8+), product categories (200+), countries (50+), quality certifications
   - Clean centered layout with large numbers
   - Keys: `SectorsPage.stats.*`

5. **CTA Section**: Use CTASection with "Looking for sourcing in your industry?" messaging.

6. **Translation updates** for all 4 locales under `SectorsPage` namespace:
   - `hero.title`, `hero.subtitle`, `hero.badge`
   - `sectors.food.title`, `sectors.food.description` (and same for all 8 sectors: food, textile, machinery, chemicals, construction, raw-materials, electronics, automotive)
   - `stats.*` keys
   - `cta.*` keys
   - `explore` (button/hover text)
   - Remove old `comingSoon` key
   - Sector descriptions should be 1-2 sentences about what Toko sources in that industry
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Sectors overview shows all 8 sectors in image-overlay grid cards. Each card links to /sectors/[slug]. Industry stats section with green accent background. All text translated in 4 locales. Build succeeds. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create sector detail pages with generateStaticParams</name>
  <files>src/app/[locale]/sectors/[slug]/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Create `src/app/[locale]/sectors/[slug]/page.tsx` as a dynamic route:

1. **Static generation**: Export `generateStaticParams()` returning all 8 sector slugs. Export `dynamicParams = false`.

2. **Metadata**: Export `generateMetadata` using `SectorDetail.[slug].metaTitle` and `SectorDetail.[slug].metaDescription`.

3. **Page structure** for each sector detail page:

   a. **PageHero**: Title from `SectorDetail.[slug].title`, subtitle from `SectorDetail.[slug].subtitle`, background image from sector data.

   b. **Breadcrumb**: Home > Sectors > [Sector Name]

   c. **Industry Overview section** (bg-white dark:bg-background):
      - Two-column layout (7/5)
      - Left: 2-3 paragraphs about the industry and Toko's role (`SectorDetail.[slug].overview.p1`, `.p2`)
      - Right: Sector image with floating stat badge
      - ScrollReveal

   d. **Products & Capabilities section** (bg-gray-50 dark:bg-card):
      - "What We Source" section title
      - Grid of product cards (from sector's productKeys)
      - Each card: product icon or bullet, translated name, brief description
      - Keys: `SectorDetail.[slug].products.[productKey].title`, `.description`

   e. **Sourcing Advantages section** (bg-amber-50 dark:bg-amber-950/20):
      - "Why Source Through Toko" for this industry
      - 3-4 advantage cards: e.g., "Verified Suppliers", "Quality Assurance", "Competitive Pricing", "Logistics Support"
      - Keys: `SectorDetail.[slug].advantages.*`

   f. **CTA Section**: Sector-specific CTA linking to /contact

4. **Error handling**: If `getSectorBySlug(slug)` returns undefined, call `notFound()`.

5. **Translation updates** for all 4 locales. Add `SectorDetail` namespace with sub-namespaces for each of the 8 sector slugs. Each slug needs:
   - `metaTitle`, `metaDescription`
   - `title`, `subtitle`
   - `overview.p1`, `overview.p2`
   - `products.[productKey].title`, `.description` (4 products each)
   - `advantages.title` + 3-4 advantage items with `.title`, `.description`
   - `sectionTitles.products`, `sectionTitles.advantages`
   - `cta.title`, `cta.description`, `cta.button`

   **IMPORTANT context-saving strategy for translations**: Since 8 sectors x 4 locales is a LOT of translation content, keep descriptions concise (1-2 sentences per field). Focus on making content unique per sector, not lengthy. For example:
   - Food sector products: grains & pulses, edible oils, dairy products, spices & herbs
   - Textile sector products: raw cotton & yarn, woven fabrics, ready-made garments, home textiles
   - etc.

6. **Params handling**: Same async params pattern as service detail pages.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>8 sector detail pages statically generated at /sectors/food, /sectors/textile, /sectors/machinery, /sectors/chemicals, /sectors/construction, /sectors/raw-materials, /sectors/electronics, /sectors/automotive. Each has PageHero, breadcrumb, overview, products grid, advantages, and CTA. Unknown slugs 404. All content translated in 4 locales. Build shows 8 sector routes x 4 locales = 32 routes. Tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` -- succeeds and shows sectors/[slug] routes for all 8 slugs x 4 locales = 32 routes
2. `npx vitest run` -- all existing tests pass
3. Sectors overview shows 8 sector cards linking to detail pages
4. All 8 sector detail pages render correctly
5. `/sectors/nonexistent` returns 404
6. All visible text from translation files
7. Translation files have `SectorDetail` namespace for all 8 sectors in all 4 locales
</verification>

<success_criteria>
- Sectors overview displays all 8 industries with image cards and links
- 8 sector detail pages statically generated via generateStaticParams
- Each detail page has unique, industry-specific content
- Green accent (emerald) used for industry stats section (color variety)
- All content translated in TR, EN, FR, RU
- dynamicParams = false on detail pages
- Build passes, tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-03-SUMMARY.md`
</output>
