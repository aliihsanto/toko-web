---
phase: 02-core-pages
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/services/page.tsx
  - src/app/[locale]/services/[slug]/page.tsx
  - src/messages/tr.json
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/ru.json
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "Services overview page lists all 4 service categories with images, descriptions, and links to detail pages"
    - "Each service has its own detail page at /services/[slug] with unique content in all 4 languages"
    - "Unknown service slugs return 404 (dynamicParams = false)"
    - "Service detail pages show hero, overview, features, process steps, and CTA"
  artifacts:
    - path: "src/app/[locale]/services/page.tsx"
      provides: "Enhanced services overview with PageHero and detail links"
      min_lines: 80
    - path: "src/app/[locale]/services/[slug]/page.tsx"
      provides: "Service detail page with generateStaticParams"
      min_lines: 100
      contains: "generateStaticParams"
      exports: ["generateStaticParams", "generateMetadata"]
  key_links:
    - from: "src/app/[locale]/services/page.tsx"
      to: "/services/[slug]"
      via: "Link on each service card"
      pattern: "Link.*href.*services/"
    - from: "src/app/[locale]/services/[slug]/page.tsx"
      to: "src/data/services.ts"
      via: "Import getServiceBySlug for data lookup"
      pattern: "import.*getServiceBySlug.*from.*@/data/services"
    - from: "src/app/[locale]/services/[slug]/page.tsx"
      to: "next-intl"
      via: "getTranslations for ServiceDetail namespace"
      pattern: "getTranslations.*ServiceDetail"
---

<objective>
Enhance the services overview page and create dynamic service detail pages for all 4 service types (import, export, sourcing, transit-trade).

Purpose: Visitors need to understand each service Toko offers in detail. The overview provides discovery, and detail pages provide the depth needed for lead conversion. Each detail page is a potential SEO landing page.
Output: Enhanced services overview, 4 service detail pages (statically generated), translations for all 4 locales.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (shared infrastructure) -->
From src/data/services.ts:
```typescript
export interface ServiceItem {
  slug: string;
  iconName: string;
  image: string;
  featureKeys: string[];
  color: string;
}
export const services: ServiceItem[];
export function getServiceBySlug(slug: string): ServiceItem | undefined;
export function getAllServiceSlugs(): string[];
```

From src/components/common/page-hero.tsx:
```typescript
interface PageHeroProps {
  title: string;
  subtitle: string;
  backgroundImage?: string;
  badge?: string;
}
export function PageHero(props: PageHeroProps): JSX.Element;
```

From src/components/common/cta-section.tsx:
```typescript
interface CTASectionProps {
  title: string;
  description: string;
  buttonText: string;
  buttonHref: string;
  note?: string;
}
export function CTASection(props: CTASectionProps): JSX.Element;
```

From src/components/common/breadcrumb.tsx:
```typescript
interface BreadcrumbItem { label: string; href?: string; }
export function Breadcrumb({ items }: { items: BreadcrumbItem[] }): JSX.Element;
```

<!-- Established patterns -->
- Pages use: getTranslations, setRequestLocale from 'next-intl/server'
- Dynamic routes use: generateStaticParams returning slug array
- Dynamic routes use: export const dynamicParams = false
- Images: next/image with Unsplash URLs
- Animations: ScrollReveal wrapper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance services overview page with PageHero and detail links</name>
  <files>src/app/[locale]/services/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Rewrite `src/app/[locale]/services/page.tsx` to be a complete services overview page:

1. **PageHero**: Use the PageHero component with translated title "Our Services", subtitle about comprehensive trade solutions, and a relevant background image (logistics/trade). Badge: "Comprehensive Solutions".

2. **Breadcrumb**: Below hero, show Home > Services.

3. **Services Grid**: Import `services` from `@/data/services`. Display all 4 services in a full-width alternating layout (odd services: image left / text right, even: image right / text left -- keep the existing alternating pattern which already works well). Each service card must:
   - Show the service image via next/image
   - Display service icon from lucide-react (dynamically map iconName to component)
   - Show translated title and description from `ServicesPage.services.[slug].title` and `ServicesPage.services.[slug].description`
   - List 3-5 features with CheckCircle2 icons using `ServicesPage.services.[slug].features.*` translation keys
   - Link to `/services/${service.slug}` with a "Learn More" button (amber CTA)
   - Use the service's `color` for accent tints on the icon background

4. **Why Choose Our Services section** (bg-[#1e3043] dark:bg-[#14202e]):
   - 3 value propositions in a row: "End-to-End Support", "Industry Expertise", "Global Network"
   - Each with icon, title, description
   - Add keys under `ServicesPage.whyChoose` namespace

5. **CTA Section**: Use CTASection component with "Need a custom trade solution?" messaging.

6. **Translation updates** for all 4 locales under `ServicesPage` namespace:
   - Expand existing translations. Keep the existing `features.*` keys.
   - Add `ServicesPage.hero.title`, `ServicesPage.hero.subtitle`, `ServicesPage.hero.badge`
   - Add `ServicesPage.services.import.title`, `.description`, `.features.customs`, `.features.logistics`, `.features.costOptimization`, `.features.supplierSelection`, `.features.qualityInspection` (5 features per service)
   - Same pattern for export, sourcing, transit-trade slugs
   - Add `ServicesPage.whyChoose.*` keys
   - Add `ServicesPage.cta.*` keys
   - Add `ServicesPage.learnMore` for button text
   - Remove old `comingSoon` key
   - All content must be substantive (real descriptions, not placeholder text)

7. **Icon mapping**: Create a local helper or use a mapping object to convert service `iconName` strings (like 'Ship', 'TrendingUp') to actual lucide-react components. Example:
   ```typescript
   import { Ship, TrendingUp, PackageCheck, BarChart3 } from 'lucide-react';
   const iconMap: Record<string, React.ComponentType<{ className?: string }>> = {
     Ship, TrendingUp, PackageCheck, BarChart3,
   };
   ```
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Services overview has PageHero, breadcrumb, 4 service cards with detail links, "Why Choose" section, and CTA. All text translated in 4 locales. Service cards link to /services/[slug]. Build succeeds. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create service detail pages with generateStaticParams</name>
  <files>src/app/[locale]/services/[slug]/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Create `src/app/[locale]/services/[slug]/page.tsx` as a dynamic route:

1. **Static generation**: Export `generateStaticParams()` returning all service slugs from `services` array. Export `dynamicParams = false` to 404 unknown slugs.

2. **Metadata**: Export `generateMetadata` that reads `ServiceDetail.[slug].metaTitle` and `ServiceDetail.[slug].metaDescription` from translations.

3. **Page structure** (each service detail page has these sections):

   a. **PageHero**: Title from `ServiceDetail.[slug].title`, subtitle from `ServiceDetail.[slug].subtitle`, background image from service data, badge from `ServiceDetail.[slug].badge`.

   b. **Breadcrumb**: Home > Services > [Service Name]

   c. **Overview section** (bg-white dark:bg-background):
      - Two-column layout (7/5)
      - Left: 2-3 paragraphs of service overview text (`ServiceDetail.[slug].overview.p1`, `.p2`, `.p3`)
      - Right: Service image with decorative border/shadow
      - ScrollReveal on both columns

   d. **Features section** (bg-gray-50 dark:bg-card):
      - Section title: "What We Offer" or similar
      - Grid of 5 feature cards (from service's featureKeys)
      - Each card: icon, translated title, translated description
      - Use the service's accent `color` for icon tinting
      - Keys: `ServiceDetail.[slug].features.[featureKey].title`, `.description`

   e. **Process section** (bg-amber-50 dark:bg-amber-950/20 for warm variety):
      - "How It Works" section with 4 numbered steps
      - Step cards with number badge, title, description
      - Keys: `ServiceDetail.[slug].process.step1.title`, `.description` (through step4)

   f. **CTA Section**: Use CTASection with service-specific CTA text (`ServiceDetail.[slug].cta.title`, etc.) linking to /contact

4. **Error handling**: If `getServiceBySlug(slug)` returns undefined, call `notFound()`.

5. **Translation updates** for all 4 locales. Add `ServiceDetail` namespace with sub-namespaces for each slug (import, export, sourcing, transit-trade). Each slug needs:
   - `metaTitle`, `metaDescription`
   - `title`, `subtitle`, `badge`
   - `overview.p1`, `overview.p2`, `overview.p3`
   - `features.[featureKey].title`, `features.[featureKey].description` (5 features)
   - `process.step1.title`, `.description` through `process.step4.title`, `.description`
   - `cta.title`, `cta.description`, `cta.button`
   - `sectionTitles.features`, `sectionTitles.process`

   Content must be substantive and unique per service -- not generic. For example:
   - Import: Focus on customs clearance, supplier vetting, logistics coordination, cost optimization
   - Export: Focus on market entry, buyer matching, documentation, trade finance
   - Sourcing: Focus on supplier audits, quality control, factory visits, negotiation
   - Transit Trade: Focus on Russia/CIS corridors, re-export, warehousing, compliance

   Turkish content must read naturally. Russian content must use proper Cyrillic with correct grammar.

6. **Params handling**: Use `params: Promise<{ locale: string; slug: string }>` pattern (Next.js 16 async params). Await params at the top of the component.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 service detail pages statically generated at /services/import, /services/export, /services/sourcing, /services/transit-trade. Each has PageHero, breadcrumb, overview, features grid, process steps, and CTA. Unknown slugs 404. All content translated in 4 locales with substantive text. Build shows all 4 service routes generated. Tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` -- succeeds and shows services/[slug] routes for all 4 slugs x 4 locales = 16 routes
2. `npx vitest run` -- all existing tests pass
3. Services overview links to individual service detail pages
4. `/services/import`, `/services/export`, `/services/sourcing`, `/services/transit-trade` all render
5. `/services/nonexistent` returns 404
6. All visible text comes from translation files (no hardcoded strings)
7. Translation files have complete `ServiceDetail` namespace for all 4 services in all 4 locales
</verification>

<success_criteria>
- Services overview page is complete with hero, service cards, value propositions, and CTA
- 4 service detail pages are statically generated via generateStaticParams
- Each detail page has 5 distinct sections with varied backgrounds
- All content translated in TR, EN, FR, RU with substantive text
- dynamicParams = false prevents unknown slug rendering
- Build passes, tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-02-SUMMARY.md`
</output>
