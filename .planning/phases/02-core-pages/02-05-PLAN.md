---
phase: 02-core-pages
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
  - PAGE-07
  - PAGE-08

must_haves:
  truths:
    - "All pages complete and visually verified by user across all locales"
    - "Color variety confirmed across all pages (amber, emerald, gray, navy -- not just blue/white)"
    - "All 8 PAGE requirements are met as confirmed by user"
  artifacts: []
  key_links: []
---

<objective>
Visually verify all Phase 2 core pages across locales, themes, and viewport sizes.

Purpose: Automated tests verify structure and build correctness, but visual quality, color variety, translation readability, and responsive behavior require human review. This checkpoint gates Phase 2 completion.
Output: User-approved visual state of all Phase 2 pages.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md
@.planning/phases/02-core-pages/02-02-SUMMARY.md
@.planning/phases/02-core-pages/02-03-SUMMARY.md
@.planning/phases/02-core-pages/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and test suite to confirm all plans integrated correctly</name>
  <files>none</files>
  <action>
Run the full build and test suite to confirm all Phase 2 work is integrated:

1. Run `npx tsc --noEmit` to verify no TypeScript errors
2. Run `npm run build` to verify all pages build (should show 64+ statically generated routes)
3. Run `npx vitest run --reporter=verbose` to verify all tests pass (Phase 1 + Phase 2 stubs)
4. Start dev server with `npm run dev` for the visual checkpoint

If any step fails, diagnose and fix the issue before proceeding to visual verification.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript passes. Build succeeds with all Phase 2 routes. All tests pass. Dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 2 core pages</name>
  <files>none</files>
  <action>
Present the user with the full Phase 2 verification checklist. All 8 page requirements for Phase 2 were built: enhanced homepage (PAGE-01), about page (PAGE-02), services overview (PAGE-03), 4 service detail pages (PAGE-04), sectors overview (PAGE-05), 8 sector detail pages (PAGE-06), references page (PAGE-07), and Russia transit trade page (PAGE-08). Total: 16 unique page templates across 4 locales = 64 locale-specific pages.

Verification steps for user:

1. Homepage (http://localhost:3000/en/) -- Hero, services cards linking to /services/[slug], trust signals, Why Us (dark navy), sectors preview (amber background), global reach, CTA

2. About (http://localhost:3000/en/about) -- PageHero, company overview, vision and mission cards, values (amber background), stats (dark navy), CTA

3. Services Overview (http://localhost:3000/en/services) -- PageHero, 4 service cards with "Learn More" links to detail pages

4. Service Detail (http://localhost:3000/en/services/import) -- Check all 4: /import, /export, /sourcing, /transit-trade. Each has unique content. Try /services/fake -- should 404.

5. Sectors Overview (http://localhost:3000/en/sectors) -- 8 sector image cards with hover effects linking to detail pages

6. Sector Detail (http://localhost:3000/en/sectors/food) -- Check a few: /food, /textile, /machinery. Each has industry-specific content.

7. References (http://localhost:3000/en/references) -- Stats, industry icons, testimonial cards, global presence

8. Russia Transit (http://localhost:3000/en/russia-transit) -- Distinct visual feel, trade corridor imagery, FAQ section, specialized CTAs

9. Language check -- Switch to /tr/, /fr/, /ru/ and verify translations appear correctly

10. Dark mode -- Toggle theme on all pages

11. Mobile -- Resize to mobile width and verify responsive layouts

Wait for user response. If user reports issues, fix them before proceeding.
  </action>
  <verify>User types "approved" or describes specific issues to fix</verify>
  <done>User has visually verified all Phase 2 pages across locales, themes, and viewport sizes. All 8 PAGE requirements confirmed working. Color variety confirmed (amber, emerald, gray, navy -- not just blue/white). Phase 2 complete and ready for Phase 3.</done>
</task>

</tasks>

<verification>
1. Full build succeeds with 64+ statically generated routes
2. All vitest tests pass (Phase 1 + Phase 2)
3. User visually approves all 8 PAGE requirements
4. Color variety confirmed across pages
5. Translations verified in all 4 locales
</verification>

<success_criteria>
- User has visually verified all Phase 2 pages across locales, themes, and viewport sizes
- All 8 PAGE requirements confirmed working
- Color variety confirmed (amber, emerald, gray, navy -- not just blue/white)
- Phase 2 complete and ready for Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-05-SUMMARY.md`
</output>
