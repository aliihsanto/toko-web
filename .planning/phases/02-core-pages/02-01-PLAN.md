---
phase: 02-core-pages
plan: 01
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - src/data/services.ts
  - src/data/sectors.ts
  - src/components/common/page-hero.tsx
  - src/components/common/cta-section.tsx
  - src/components/common/breadcrumb.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/about/page.tsx
  - src/messages/tr.json
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/ru.json
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "Homepage displays hero, services overview cards linking to /services/[slug], sectors preview, trust signals, and CTAs in the visitor's language"
    - "About page presents company history timeline, vision, mission, and values -- all translated in 4 languages"
    - "Every content page can render a hero section, CTA banner, and breadcrumb navigation without duplicating code"
    - "Service and sector items are browsable by slug from any page that needs them"
  artifacts:
    - path: "src/data/services.ts"
      provides: "Service item definitions with slugs, icons, images, accent colors"
      exports: ["services", "getServiceBySlug", "getAllServiceSlugs", "ServiceItem"]
    - path: "src/data/sectors.ts"
      provides: "Sector item definitions with slugs, icons, images"
      exports: ["sectors", "getSectorBySlug", "getAllSectorSlugs", "SectorItem"]
    - path: "src/components/common/page-hero.tsx"
      provides: "Reusable hero section for content pages"
      exports: ["PageHero"]
    - path: "src/components/common/cta-section.tsx"
      provides: "Reusable CTA banner section"
      exports: ["CTASection"]
    - path: "src/components/common/breadcrumb.tsx"
      provides: "Breadcrumb navigation component"
      exports: ["Breadcrumb"]
    - path: "src/app/[locale]/page.tsx"
      provides: "Enhanced homepage with sectors preview and trust signals"
      min_lines: 200
    - path: "src/app/[locale]/about/page.tsx"
      provides: "Full about page with history, vision, mission, values"
      min_lines: 150
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "/services/[slug]"
      via: "Link component on service cards"
      pattern: "Link.*href.*services/"
    - from: "src/app/[locale]/page.tsx"
      to: "/sectors"
      via: "Sectors preview section linking to overview"
      pattern: "Link.*href.*sectors"
    - from: "src/data/services.ts"
      to: "src/app/[locale]/page.tsx"
      via: "Import services array for homepage cards"
      pattern: "import.*from.*@/data/services"
---

<objective>
Create shared infrastructure (reusable components and data files) needed by all Phase 2 pages, then enhance the homepage and build the full about page.

Purpose: This plan establishes the foundation for all subsequent Phase 2 plans. Shared components (PageHero, CTASection, Breadcrumb) and data files (services.ts, sectors.ts) prevent duplication. The homepage gets linked service cards and new sections. The about page replaces its placeholder with complete content.
Output: 5 new shared files, enhanced homepage, full about page, updated translations in all 4 locales.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-RESEARCH.md

<interfaces>
<!-- Existing components and utilities from Phase 1 -->
From src/components/common/scroll-reveal.tsx:
```typescript
interface ScrollRevealProps {
  children: React.ReactNode;
  className?: string;
  delay?: number;
  direction?: 'up' | 'down' | 'left' | 'right';
}
export function ScrollReveal({ children, className, delay, direction }: ScrollRevealProps): JSX.Element;
```

From src/i18n/navigation.ts:
```typescript
export const Link: typeof NextLink; // Locale-aware Link
export const useRouter: () => AppRouterInstance;
export const usePathname: () => string;
export const redirect: typeof NextRedirect;
export const getPathname: (args: { locale: string; href: string }) => string;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/components/ui/button.tsx:
```typescript
export const Button: React.ForwardRefExoticComponent<ButtonProps>;
```

<!-- Established patterns -->
- Server components use: getTranslations, setRequestLocale from 'next-intl/server'
- Client components use: useTranslations from 'next-intl'
- All pages call setRequestLocale(locale) first
- ScrollReveal wraps sections for animation
- oklch color system in Tailwind v4
- Image optimization via next/image with Unsplash remotePatterns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared components and data files</name>
  <files>src/data/services.ts, src/data/sectors.ts, src/components/common/page-hero.tsx, src/components/common/cta-section.tsx, src/components/common/breadcrumb.tsx</files>
  <action>
1. Create `src/data/services.ts` with:
   - `ServiceItem` interface: `{ slug: string; iconName: string; image: string; featureKeys: string[]; color: string }`
   - 4 services: import, export, sourcing, transit-trade (matching existing homepage keys)
   - Use high-quality Unsplash images (different from homepage to avoid reuse):
     - import: container ship/port image
     - export: cargo loading/logistics image
     - sourcing: warehouse/factory image
     - transit-trade: truck/route/corridor image
   - Each service has 5 feature keys (translation keys, not actual text)
   - `color` field: 'blue' for import, 'emerald' for export, 'amber' for sourcing, 'rose' for transit-trade
   - Export: `getServiceBySlug(slug)`, `getAllServiceSlugs()`, `services` array

2. Create `src/data/sectors.ts` with:
   - `SectorItem` interface: `{ slug: string; iconName: string; image: string; productKeys: string[] }`
   - 8 sectors (expanded from 4): food, textile, machinery, chemicals, construction, raw-materials, electronics, automotive
   - Use relevant Unsplash images for each sector
   - 4 product keys per sector (translation keys)
   - Export: `getSectorBySlug(slug)`, `getAllSectorSlugs()`, `sectors` array

3. Create `src/components/common/page-hero.tsx` (server component, no 'use client'):
   - Props: `{ title: string; subtitle: string; backgroundImage?: string; badge?: string }`
   - Dark navy background (bg-[#1e3043] dark:bg-[#14202e]) matching existing hero sections
   - Optional background image with 30% opacity overlay
   - Optional badge pill (amber accent)
   - Min height 40vh, centered content with max-w-7xl
   - Import ScrollReveal for fade-in animation on title and subtitle

4. Create `src/components/common/cta-section.tsx` (server component):
   - Props: `{ title: string; description: string; buttonText: string; buttonHref: string; note?: string }`
   - Dark navy background with dot pattern (matching existing CTA section pattern)
   - Centered layout with amber CTA button
   - Import Link from @/i18n/navigation, Button, ScrollReveal

5. Create `src/components/common/breadcrumb.tsx` (server component):
   - Props: `{ items: Array<{ label: string; href?: string }> }`
   - Uses Link from @/i18n/navigation for locale-aware links
   - ChevronRight separator from lucide-react
   - Semantic nav with aria-label="Breadcrumb"
   - Muted text color, small text size
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 files created with correct exports and TypeScript types. No type errors. Services array has 4 items, sectors array has 8 items. Components accept documented props.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance homepage with sectors preview, trust signals, and linked service cards</name>
  <files>src/app/[locale]/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Enhance the existing homepage (do NOT rebuild from scratch -- keep existing hero, services, whyUs, global reach, and CTA sections). Make these specific changes:

1. **Import data from services.ts**: Replace the inline services array in the services section with `import { services } from '@/data/services'`. Map service cards to link to `/services/${service.slug}` instead of `/services`. Keep the same visual card layout but add "Learn More" arrow text at the bottom of each card.

2. **Add Sectors Preview section** (between "Global Reach" and "CTA" sections):
   - Import first 4 sectors from `@/data/sectors`
   - Background: bg-amber-50 dark:bg-amber-950/20 (warm accent -- breaks blue/white monotony per user feedback)
   - Section header: "Sectors We Serve" (translated)
   - Grid of 4 sector image cards (same overlay-card style as current sectors page)
   - "View All Sectors" button linking to /sectors
   - Add translation keys under `HomePage.sectors` namespace: `title`, `subtitle`, `viewAll`

3. **Add Trust Signals section** (between "Services" and "Why Us" sections):
   - Background: bg-gray-50 dark:bg-card
   - Show 4 trust metrics in a centered row: "1000+ Clients", "50+ Countries", "20+ Years", "ISO 9001" -- use translated labels
   - Simple centered layout with large numbers and small labels
   - Add translation keys under `HomePage.trust` namespace

4. **Update service card links**: Each service card now links to `/services/${slug}` (import, export, sourcing, transit-trade). Use `import { services } from '@/data/services'` for the data. The `key` field on existing service objects maps to slug (import->import, export->export, sourcing->sourcing, transit->transit-trade).

5. **Color variety**: Ensure the section backgrounds alternate properly:
   - Hero: dark overlay image (existing)
   - Services: white/background (existing)
   - Trust Signals: gray-50/card (new)
   - Why Us: dark navy (existing)
   - Sectors Preview: amber-50/amber-950 (new -- warm accent)
   - Global Reach: gray-50/card (existing)
   - CTA: dark navy (existing)

6. **Translation updates** for all 4 locales (tr.json, en.json, fr.json, ru.json):
   - Add `HomePage.sectors.title`, `HomePage.sectors.subtitle`, `HomePage.sectors.viewAll`
   - Add `HomePage.trust.title` and `HomePage.trust.items` (clients, countries, years, certification labels)
   - TR translations must be natural Turkish (not machine-translated feel)
   - RU translations must use proper Cyrillic
   - FR translations must be grammatically correct French
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx tsc --noEmit && npx vitest run src/__tests__/homepage.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Homepage has 7 distinct sections with alternating backgrounds (no more "too much blue/white"). Service cards link to /services/[slug]. Sectors preview shows 4 sectors with warm amber background. Trust signals section visible. All text translated in 4 locales. TypeScript passes. Homepage tests run (todos skipped, no failures).</done>
</task>

<task type="auto">
  <name>Task 3: Build full about page with company history, vision, mission, and values</name>
  <files>src/app/[locale]/about/page.tsx, src/messages/tr.json, src/messages/en.json, src/messages/fr.json, src/messages/ru.json</files>
  <action>
Replace the placeholder about page with a full content page. Structure:

1. **PageHero section**: Use the new PageHero component with translated title "About Toko Trading", subtitle about the company, and a relevant Unsplash background image (e.g., Istanbul skyline or Bosphorus bridge for Turkish trade company identity). Badge: "Since 2003" or similar.

2. **Company Overview section** (bg-white dark:bg-background):
   - Two-column layout (7/5 grid like existing pattern)
   - Left: Translated company description paragraphs (2-3 paragraphs about Toko's history, founding, growth)
   - Right: Image with floating stat badge (e.g., "20+ Years" like existing pattern)
   - Use ScrollReveal on both columns

3. **Vision & Mission section** (bg-gray-50 dark:bg-card):
   - Two equal cards side by side
   - Vision card: icon (Eye or Compass from lucide), title, description paragraph
   - Mission card: icon (Target), title, description paragraph
   - Cards have subtle border and hover effect
   - Use amber accent for icons

4. **Company Values section** (bg-amber-50 dark:bg-amber-950/20 for warm variety):
   - 4 value cards in a grid: Reliability, Transparency, Innovation, Partnership (or similar B2B values)
   - Each card: lucide icon, title, description
   - Use varied icon colors (amber, emerald, blue, rose) for visual variety

5. **Key Achievements / Stats section** (bg-[#1e3043] dark:bg-[#14202e]):
   - Dark navy section with 4 stat counters
   - Similar glass card pattern to homepage stats
   - Stats: years experience, countries served, clients, team members

6. **CTA Section**: Use the new CTASection component with "Ready to partner?" messaging linking to /contact

7. **Breadcrumb**: Add Breadcrumb component at the top of the content (below PageHero) showing Home > About Us

8. **Remove all hardcoded text**: The existing about page has hardcoded strings like "Vision", "Mission", "Professional Approach", "Years" -- ALL must be replaced with translation keys.

9. **Translation updates** for all 4 locales under `AboutPage` namespace:
   - `hero.title`, `hero.subtitle`, `hero.badge`
   - `overview.p1`, `overview.p2`, `overview.p3` (company description paragraphs)
   - `vision.title`, `vision.description`
   - `mission.title`, `mission.description`
   - `values.title`, `values.subtitle`
   - `values.reliability.title`, `values.reliability.description` (and 3 more values)
   - `stats.years`, `stats.countries`, `stats.clients`, `stats.team` (labels)
   - `cta.title`, `cta.description`, `cta.button`, `cta.note`
   - `breadcrumb.home`, `breadcrumb.about` (or use existing Common translations)
   - Content should be realistic and professional. Turkish content should feel native, not translated.
   - Remove the old `comingSoon` key from AboutPage namespace.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npx tsc --noEmit && npx vitest run src/__tests__/about-page.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>About page has 6 distinct sections: PageHero, Company Overview, Vision & Mission, Values (amber background), Stats (dark navy), CTA. No hardcoded text -- all from translations. Breadcrumb shows navigation path. All 4 locales have complete AboutPage translations. TypeScript passes. About page tests run (todos skipped, no failures).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no TypeScript errors
2. `npm run build` -- build succeeds with all locale routes
3. `npx vitest run src/__tests__/homepage.test.tsx src/__tests__/about-page.test.tsx --reporter=verbose` -- tests run without failures
4. Homepage has 7 sections with varied backgrounds (not just blue/white)
5. Service cards on homepage link to `/services/[slug]`
6. About page has no hardcoded English text
7. Translation files have complete `AboutPage` and expanded `HomePage` namespaces in all 4 locales
8. Data files export correct types and helper functions
</verification>

<success_criteria>
- Homepage enhanced with sectors preview (amber background), trust signals, and linked service cards
- About page fully built with 6 content sections, no placeholder/skeleton content
- 5 shared infrastructure files created (page-hero, cta-section, breadcrumb, services data, sectors data)
- All visible text translated in TR, EN, FR, RU
- Build passes, all existing tests pass
- No regressions in existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-01-SUMMARY.md`
</output>
