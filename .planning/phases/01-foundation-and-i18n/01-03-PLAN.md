---
phase: 01-foundation-and-i18n
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/language-switcher.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/__tests__/header.test.tsx
  - src/__tests__/language-switcher.test.tsx
autonomous: true
requirements:
  - UX-03
  - I18N-03
  - UX-02

must_haves:
  truths:
    - "Header displays logo, navigation items, language switcher, theme toggle, and CTA button"
    - "Header is large on page load and shrinks to compact sticky nav on scroll"
    - "User can switch language via header dropdown and page content updates to selected locale"
    - "Mobile users see a hamburger menu that opens a drawer with all navigation items"
    - "Current locale is preserved when navigating between pages"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Responsive header with shrink-on-scroll behavior"
      contains: "scrolled"
      min_lines: 60
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Mobile hamburger menu with slide-in drawer"
      contains: "Sheet"
    - path: "src/components/layout/language-switcher.tsx"
      provides: "Locale switching dropdown"
      contains: "useLocale"
    - path: "src/__tests__/header.test.tsx"
      provides: "Tests for header component rendering"
    - path: "src/__tests__/language-switcher.test.tsx"
      provides: "Tests for language switcher component"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/language-switcher.tsx"
      via: "import and render in header"
      pattern: "LanguageSwitcher"
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/mobile-nav.tsx"
      via: "import and render for mobile breakpoint"
      pattern: "MobileNav"
    - from: "src/components/layout/language-switcher.tsx"
      to: "src/i18n/navigation.ts"
      via: "useRouter and usePathname for locale switching"
      pattern: "router\\.replace.*locale"
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/theme-toggle.tsx"
      via: "import and render theme toggle"
      pattern: "ThemeToggle"
---

<objective>
Build the complete header component with shrink-on-scroll behavior, desktop navigation menu, mobile hamburger drawer, language switcher dropdown, theme toggle, and localized CTA button. Wire into locale layout. Create behavioral tests for header rendering and language switcher functionality.

Purpose: The header is the most complex UI component in Phase 1 and the primary way users navigate the site and switch languages. It establishes the navigation patterns used across all pages in subsequent phases.
Output: Fully responsive header with desktop nav, mobile drawer, language switcher, theme toggle, CTA, and passing tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-i18n/01-RESEARCH.md
@.planning/phases/01-foundation-and-i18n/01-CONTEXT.md
@.planning/phases/01-foundation-and-i18n/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-i18n/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 - i18n navigation -->
From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```

From src/i18n/routing.ts:
```typescript
export const routing: { locales: ['tr', 'en', 'fr', 'ru']; defaultLocale: 'tr'; };
```

<!-- From Plan 02 - UI components and utilities -->
From src/components/layout/theme-toggle.tsx:
```typescript
export function ThemeToggle(): JSX.Element;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/types/index.ts:
```typescript
export const locales = ['tr', 'en', 'fr', 'ru'] as const;
export type Locale = (typeof locales)[number];
```

<!-- shadcn/ui components available -->
- @/components/ui/button (Button)
- @/components/ui/dropdown-menu (DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem)
- @/components/ui/sheet (Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle)
- @/components/ui/navigation-menu (NavigationMenu, NavigationMenuList, etc.)

<!-- Translation keys available in Header namespace -->
Header.nav.home, Header.nav.about, Header.nav.services, Header.nav.sectors,
Header.nav.references, Header.nav.blog, Header.nav.contact, Header.cta
LanguageSwitcher.tr, LanguageSwitcher.en, LanguageSwitcher.fr, LanguageSwitcher.ru, LanguageSwitcher.label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create language switcher, mobile navigation, and language-switcher tests</name>
  <files>src/components/layout/language-switcher.tsx, src/components/layout/mobile-nav.tsx, src/__tests__/language-switcher.test.tsx</files>
  <action>
1. Create `src/components/layout/language-switcher.tsx`:
   - Client component ('use client')
   - Use `useLocale` from `next-intl` to get current locale
   - Use `useRouter` and `usePathname` from `@/i18n/navigation` for locale switching
   - Use `useTranslations('LanguageSwitcher')` for locale display names
   - Use shadcn/ui DropdownMenu for the dropdown
   - Use Globe icon from lucide-react as trigger icon
   - Show current locale name next to icon (hidden on very small screens, visible on sm+)
   - On locale change: call `router.replace(pathname, { locale: newLocale })`
   - Highlight currently active locale in dropdown
   - Design: clean, minimal dropdown with locale names (e.g., "Turkce", "English", "Francais", "Russkiy")
   - Do NOT use flag icons (per Claude's discretion -- text-based is more professional for B2B corporate)

2. Create `src/components/layout/mobile-nav.tsx`:
   - Client component ('use client')
   - Use shadcn/ui Sheet component (slide-in from right)
   - Trigger: hamburger icon (Menu from lucide-react) wrapped in Button variant="ghost"
   - Content: full navigation list (all nav items as Link components from @/i18n/navigation)
   - Include language switcher in mobile nav (inline list, not dropdown)
   - Include theme toggle
   - Include CTA button at bottom of drawer
   - Use `useTranslations('Header')` for nav item labels
   - Close sheet when a navigation link is clicked (use Sheet's onOpenChange or a state setter)
   - Only visible on mobile (md:hidden on trigger, md:hidden on sheet)
   - Each nav item: full-width, good tap target (min h-12), with subtle hover state

   Navigation items (use Link from @/i18n/navigation for all):
   - Home: "/"
   - About: "/about" (placeholder -- pages built in Phase 2)
   - Services: "/services"
   - Sectors: "/sectors"
   - References: "/references"
   - Blog: "/blog"
   - Contact: "/contact"

   NOTE: These routes will 404 for now since only the homepage exists. That is expected -- Phase 2 creates the pages. The nav links establish the routing contract.

3. Create `src/__tests__/language-switcher.test.tsx`:
   - Mock `next-intl` (useLocale, useTranslations) and `@/i18n/navigation` (useRouter, usePathname)
   - Test that LanguageSwitcher renders a trigger button with the Globe icon
   - Test that all 4 locale names appear when dropdown is opened (click trigger, then check for "Turkce", "English", "Francais", "Russkiy" text)
   - Test that clicking a locale option calls router.replace with the correct locale parameter
   - Test that the current locale is visually distinguished (e.g., has a different style or checkmark)
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -5 && npx vitest run src/__tests__/language-switcher.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>LanguageSwitcher component switches locale while preserving current path. MobileNav component opens a slide-in drawer with all navigation items, language options, theme toggle, and CTA. Both components use translations from message files. Language switcher test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create header component with shrink-on-scroll, wire into locale layout, and add header tests</name>
  <files>src/components/layout/header.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/__tests__/header.test.tsx</files>
  <action>
1. Create `src/components/layout/header.tsx`:
   - Client component ('use client')
   - Uses useState + useEffect for scroll detection (threshold: 50px)
   - Uses `useTranslations('Header')` for nav labels and CTA text
   - Uses `cn()` for conditional class merging

   **Desktop layout (hidden on mobile, visible md+):**
   - Fixed position, full width, z-50
   - When NOT scrolled: py-6, transparent or bg-background, larger logo text
   - When scrolled: py-2, bg-background/95 backdrop-blur-sm shadow-sm, compact logo
   - Transition: all properties transition smoothly (duration-300)

   Structure (left to right):
   - Logo: "TOKO" text in bold, uppercase, tracking-wider, using Link to "/"
     - Not scrolled: text-2xl, scrolled: text-xl
   - Desktop Navigation: horizontal list of nav items using Link from @/i18n/navigation
     - Items: Home, About, Services, Sectors, References, Blog, Contact
     - Use `useTranslations('Header')` for labels (t('nav.home'), etc.)
     - Active state: detect current path with `usePathname` from @/i18n/navigation
     - Hover: subtle underline or color shift
   - Right section: LanguageSwitcher + ThemeToggle + CTA Button
     - CTA: Button variant="default" with t('cta') text, Link to "/contact"

   **Mobile layout (visible on mobile, hidden md+):**
   - Fixed header with logo on left, MobileNav trigger (hamburger) on right
   - Same shrink-on-scroll behavior

   **Content spacer:** Add a div or use padding-top on the main content area to account for the fixed header height. When not scrolled: ~80-96px, when scrolled: ~64px. Use a CSS variable or a spacer div.

   Use passive scroll listener: `addEventListener('scroll', handler, { passive: true })`
   Clean up listener on unmount.

2. Update `src/app/[locale]/layout.tsx`:
   - Import Header from `@/components/layout/header`
   - Add Header INSIDE the ThemeProvider
   - Add a spacer div or padding to main to prevent content from being hidden behind fixed header
   - Structure:
     ```
     <ThemeProvider>
       <Header />
       <main className="pt-24">{children}</main>
     </ThemeProvider>
     ```
   - The pt-24 (96px) provides space for the large header state. Adjust if needed.

3. Update `src/app/[locale]/page.tsx`:
   - Remove the temporary ThemeToggle that was added in Plan 02 (it now lives in the header)
   - Add enough content height to test scroll behavior (a tall placeholder section)
   - Keep the translated title/subtitle/description from Plan 01
   - Add a section below with min-h-screen to ensure the page is scrollable for testing shrink behavior

4. Create `src/__tests__/header.test.tsx`:
   - Mock `next-intl` (useTranslations) and `@/i18n/navigation` (Link, usePathname)
   - Mock child components: ThemeToggle, LanguageSwitcher, MobileNav (mock as simple divs with data-testid)
   - Test that Header renders the TOKO logo text
   - Test that Header renders all 7 navigation items (Home, About, Services, Sectors, References, Blog, Contact)
   - Test that Header renders LanguageSwitcher, ThemeToggle, and CTA button
   - Test that the CTA button text comes from translations (t('cta'))
   - Test that the header has aria-label or role="banner" for accessibility

5. Verify scroll behavior: Run dev server, scroll down -- header should shrink smoothly. Scroll back up -- header should expand. Language switcher should work (changing locale preserves path). Mobile view should show hamburger menu.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -5 && npx vitest run src/__tests__/header.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Header renders with logo, nav items, language switcher, theme toggle, and CTA. Header shrinks on scroll and becomes sticky. Language switcher changes locale. Mobile hamburger opens drawer with full navigation. Main content is not hidden behind fixed header. Header test passes verifying all key elements render.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Desktop: Header shows TOKO logo, 7 nav items, language switcher, theme toggle, CTA button
3. Desktop: Scrolling down causes header to shrink and become compact with backdrop blur
4. Desktop: Language switcher changes locale (URL updates, content changes)
5. Mobile (< 768px): Header shows logo + hamburger icon
6. Mobile: Hamburger opens Sheet drawer with all nav items + language options + theme toggle + CTA
7. All text comes from translation files (no hardcoded strings)
8. Navigation links use next-intl Link (locale prefix auto-applied)
9. `npx vitest run src/__tests__/header.test.tsx src/__tests__/language-switcher.test.tsx` both pass
</verification>

<success_criteria>
- Header is the primary navigation interface for the site
- Shrink-on-scroll works smoothly without layout jank
- Language switcher changes locale while preserving current path
- Mobile navigation is fully functional with proper tap targets
- All visible text is translated (no hardcoded strings)
- Theme toggle accessible from both desktop and mobile nav
- Header and language switcher tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-i18n/01-03-SUMMARY.md`
</output>
