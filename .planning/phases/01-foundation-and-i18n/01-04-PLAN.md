---
phase: 01-foundation-and-i18n
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/components/layout/footer.tsx
  - src/app/[locale]/layout.tsx
  - src/__tests__/footer.test.tsx
autonomous: true
requirements:
  - UX-04
  - UX-02

must_haves:
  truths:
    - "Footer displays 4 columns: About, Services, Blog, Contact"
    - "Footer shows company address, phone, and email"
    - "Footer displays social media icon links (LinkedIn, Twitter, Instagram)"
    - "Footer shows copyright notice with current year"
    - "Footer includes a secondary language switcher"
    - "Footer is fully responsive: 4 columns on desktop, stacks on mobile"
    - "All footer text is translated in all 4 locales"
  artifacts:
    - path: "src/components/layout/footer.tsx"
      provides: "4-column responsive footer with contact info, links, and social media"
      min_lines: 80
      contains: "useTranslations"
    - path: "src/__tests__/footer.test.tsx"
      provides: "Tests for footer component rendering"
  key_links:
    - from: "src/components/layout/footer.tsx"
      to: "src/messages/*.json"
      via: "useTranslations('Footer')"
      pattern: "useTranslations.*Footer"
    - from: "src/components/layout/footer.tsx"
      to: "src/i18n/navigation.ts"
      via: "Link component for internal links"
      pattern: "import.*Link.*navigation"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "import and render after main content"
      pattern: "Footer"
---

<objective>
Build the complete footer component with 4-column layout (About, Services, Blog, Contact), social media links, company contact information, copyright notice, and secondary language switcher. Wire it into the locale layout after the Header (which was wired in Plan 03). Create behavioral tests for footer rendering.

Purpose: The footer provides secondary navigation, company credibility signals, and contact information on every page. It completes the site shell so all subsequent phase work has proper page boundaries.
Output: Responsive 4-column footer rendered on all pages, with passing footer tests.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-i18n/01-RESEARCH.md
@.planning/phases/01-foundation-and-i18n/01-CONTEXT.md
@.planning/phases/01-foundation-and-i18n/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 - i18n navigation -->
From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } = createNavigation(routing);
```

<!-- From Plan 02 - utilities -->
From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

<!-- Translation keys available in Footer namespace -->
Footer.about.title, Footer.about.description
Footer.services.title, Footer.services.import, Footer.services.export, Footer.services.transitTrade, Footer.services.sourcing
Footer.blog.title, Footer.blog.latest, Footer.blog.categories
Footer.contact.title, Footer.contact.address, Footer.contact.phone, Footer.contact.email
Footer.copyright
Footer.social.linkedin, Footer.social.twitter, Footer.social.instagram

<!-- shadcn/ui components available -->
- @/components/ui/separator (Separator)

<!-- From Plan 03 (complete -- this plan depends on it) -->
From src/components/layout/language-switcher.tsx:
```typescript
export function LanguageSwitcher(): JSX.Element;
```

From src/app/[locale]/layout.tsx (current state after Plan 03):
- Has Header imported and rendered
- Has ThemeProvider wrapping content
- Has NextIntlClientProvider wrapping ThemeProvider
- Structure: NextIntlClientProvider > ThemeProvider > Header + main + {children}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create responsive 4-column footer with contact info, links, social media, and footer tests</name>
  <files>src/components/layout/footer.tsx, src/__tests__/footer.test.tsx</files>
  <action>
1. Create `src/components/layout/footer.tsx`:
   - Client component ('use client') -- needs useTranslations
   - Use `useTranslations('Footer')` for all text content
   - Use `Link` from `@/i18n/navigation` for all internal links
   - Use `cn()` for conditional classes

   **Layout structure:**
   ```
   <footer> (bg-muted/50 dark:bg-card border-t)
     <div> (container max-w-7xl mx-auto px-4 py-12)

       <!-- Main footer grid: 4 columns on lg, 2 on md, 1 on mobile -->
       <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">

         <!-- Column 1: About -->
         <div>
           <h3>TOKO</h3> (logo text, bold, text-xl)
           <p>{t('about.description')}</p>
           <!-- Social media icons -->
           <div className="flex gap-3 mt-4">
             <a href="https://linkedin.com" target="_blank" rel="noopener noreferrer">
               <Linkedin icon from lucide-react />
             </a>
             <a href="https://twitter.com" target="_blank" rel="noopener noreferrer">
               <Twitter icon />
             </a>
             <a href="https://instagram.com" target="_blank" rel="noopener noreferrer">
               <Instagram icon />
             </a>
           </div>
         </div>

         <!-- Column 2: Services -->
         <div>
           <h4>{t('services.title')}</h4>
           <ul>
             <li><Link href="/services">{t('services.import')}</Link></li>
             <li><Link href="/services">{t('services.export')}</Link></li>
             <li><Link href="/services">{t('services.transitTrade')}</Link></li>
             <li><Link href="/services">{t('services.sourcing')}</Link></li>
           </ul>
         </div>

         <!-- Column 3: Blog -->
         <div>
           <h4>{t('blog.title')}</h4>
           <ul>
             <li><Link href="/blog">{t('blog.latest')}</Link></li>
             <li><Link href="/blog">{t('blog.categories')}</Link></li>
           </ul>
         </div>

         <!-- Column 4: Contact -->
         <div>
           <h4>{t('contact.title')}</h4>
           <address className="not-italic">
             <p>{t('contact.address')}</p>
             <p><a href="tel:+902120000000">{t('contact.phone')}</a></p>
             <p><a href="mailto:info@toko.com.tr">{t('contact.email')}</a></p>
           </address>
         </div>
       </div>

       <!-- Separator -->
       <Separator className="my-8" />

       <!-- Bottom bar: copyright + language links -->
       <div className="flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-muted-foreground">
         <p>&copy; {t('copyright')}</p>

         <!-- Simple language links (secondary switcher) -->
         <div className="flex gap-4">
           <!-- For each locale, render a simple text link that switches locale -->
           <!-- Use useLocale to highlight current, useRouter/usePathname for switching -->
           {locales.map(loc => (
             <button onClick={() => router.replace(pathname, { locale: loc })}
               className={cn("hover:text-primary", locale === loc && "text-primary font-medium")}
             >
               {localeNames[loc]}
             </button>
           ))}
         </div>
       </div>
     </div>
   </footer>
   ```

   Styling details:
   - Footer headings (h4): font-semibold, text-foreground, mb-4
   - Footer links: text-muted-foreground, hover:text-primary transition-colors
   - Social icons: h-5 w-5, text-muted-foreground, hover:text-primary
   - Footer link lists: space-y-2
   - Responsive: on mobile, columns stack cleanly with good spacing
   - Use `useLocale`, `useRouter`, `usePathname` from next-intl/navigation for the secondary language switcher at the bottom

2. The footer language switcher should be simple text links (not a dropdown like the header) -- this is the secondary switcher for users who scroll to the bottom.

3. Create `src/__tests__/footer.test.tsx`:
   - Mock `next-intl` (useTranslations, useLocale) and `@/i18n/navigation` (Link, useRouter, usePathname)
   - Test that Footer renders the TOKO logo text in the about column
   - Test that Footer renders all 4 column headings (About description, Services title, Blog title, Contact title)
   - Test that Footer renders social media links (3 external links with target="_blank")
   - Test that Footer renders contact info (address, phone, email)
   - Test that Footer renders copyright text
   - Test that Footer renders language switcher buttons for all 4 locales
   - Test that the footer has semantic footer element (role="contentinfo" or <footer> tag)
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -5 && npx vitest run src/__tests__/footer.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Footer renders with 4 columns (About+social, Services links, Blog links, Contact info). Social media icons link externally. Copyright shows. Secondary language switcher at bottom allows locale switching. All text from translation files. Responsive: 4 cols on lg, 2 on md, 1 on mobile. Footer test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire footer into locale layout and verify full page shell</name>
  <files>src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx</files>
  <action>
1. Update `src/app/[locale]/layout.tsx`:
   - Import Footer from `@/components/layout/footer`
   - Add Footer AFTER the main content area, still inside ThemeProvider
   - Since Plan 03 already wired Header, the layout should now have both:
     ```
     <ThemeProvider>
       <Header />
       <main className="min-h-screen pt-24">{children}</main>
       <Footer />
     </ThemeProvider>
     ```

   IMPORTANT: Read the current state of layout.tsx before modifying. Preserve all existing code (i18n, ThemeProvider, Header, etc.). Only ADD the Footer import and render.

2. Update `src/app/[locale]/page.tsx`:
   - Ensure the homepage has enough visual content to verify the full page shell looks correct
   - Keep the translated title, subtitle, description
   - Add a visual section showing that the footer is properly positioned at the bottom:
     ```
     <section className="flex min-h-[60vh] flex-col items-center justify-center px-4">
       <h1 className="text-4xl font-bold tracking-tight">{t('title')}</h1>
       <p className="mt-4 text-xl text-muted-foreground">{t('subtitle')}</p>
       <p className="mt-2 text-muted-foreground">{t('description')}</p>
     </section>
     ```
   - This ensures the page has enough height that the footer appears at the natural bottom

3. Test by running dev server: verify footer renders on all 4 locale pages with translated content. Verify responsive behavior at different viewport widths.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/Desktop/Toko Web" && npm run build 2>&1 | tail -5 && npx vitest run src/__tests__/footer.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Footer renders on all locale pages. Layout structure is complete: Header + main content + Footer. All text is translated. Page shell is ready for Phase 2 content pages to drop into the main area.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Footer visible on all 4 locale pages (/tr, /en, /fr, /ru) with translated text
3. Footer shows 4 columns on desktop (lg+), 2 columns on tablet (md), single column on mobile
4. Social media icons render and link to external URLs
5. Secondary language switcher at footer bottom works (changes locale)
6. Copyright notice displays
7. Contact info (address, phone, email) displayed with proper links
8. All footer text comes from translation files
9. `npx vitest run src/__tests__/footer.test.tsx` passes
</verification>

<success_criteria>
- Footer renders on every page via locale layout
- 4-column responsive grid works at all breakpoints
- Social media links open in new tab
- Contact info includes address, phone (tel: link), email (mailto: link)
- Secondary language switcher works
- All text translated, no hardcoded strings
- Page shell complete: content sits between header and footer
- Footer test passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-i18n/01-04-SUMMARY.md`
</output>
