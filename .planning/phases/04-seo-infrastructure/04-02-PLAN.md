---
phase: 04-seo-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/json-ld.tsx
  - src/lib/seo/structured-data.ts
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/__tests__/json-ld.test.ts
  - src/__tests__/sitemap.test.ts
  - src/__tests__/robots.test.ts
  - package.json
autonomous: true
requirements:
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Every page renders Organization and BreadcrumbList JSON-LD in the HTML"
    - "Russia-transit page renders FAQPage JSON-LD with locale-aware Q&A pairs"
    - "sitemap.xml lists all pages across all 4 locales with hreflang alternates"
    - "robots.txt allows all crawlers, disallows /api/, and points to sitemap.xml"
  artifacts:
    - path: "src/lib/seo/json-ld.tsx"
      provides: "Reusable JsonLd server component with XSS protection"
      exports: ["JsonLd"]
    - path: "src/lib/seo/structured-data.ts"
      provides: "Schema builder functions for Organization, LocalBusiness, BreadcrumbList, FAQPage"
      exports: ["getOrganizationSchema", "getLocalBusinessSchema", "getBreadcrumbSchema", "getFAQSchema"]
    - path: "src/app/sitemap.ts"
      provides: "Multilingual sitemap with all pages and hreflang alternates"
    - path: "src/app/robots.ts"
      provides: "Robots configuration"
    - path: "src/__tests__/json-ld.test.ts"
      provides: "Tests for JSON-LD component and structured data builders"
    - path: "src/__tests__/sitemap.test.ts"
      provides: "Tests for sitemap entries and alternates coverage"
    - path: "src/__tests__/robots.test.ts"
      provides: "Tests for robots configuration"
  key_links:
    - from: "src/lib/seo/structured-data.ts"
      to: "src/lib/seo/company-info.ts"
      via: "imports COMPANY_INFO for Organization/LocalBusiness schemas"
      pattern: "import.*COMPANY_INFO.*from.*company-info"
    - from: "src/app/sitemap.ts"
      to: "src/data/services.ts"
      via: "imports services array for dynamic service page URLs"
      pattern: "import.*services.*from.*@/data/services"
    - from: "src/app/sitemap.ts"
      to: "src/data/sectors.ts"
      via: "imports sectors array for dynamic sector page URLs"
      pattern: "import.*sectors.*from.*@/data/sectors"
---

<objective>
Create JSON-LD structured data components, a multilingual sitemap, and robots.ts for the entire site.

Purpose: SEO-02/03 requires JSON-LD structured data on all pages. SEO-04/05 require a sitemap covering all pages x all locales with hreflang alternates, and properly configured robots.txt. These are independent of per-page generateMetadata (plan 01) and can be built in parallel.

Output: JsonLd component, structured data builder functions, sitemap.ts, robots.ts, and comprehensive tests for all.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-infrastructure/04-RESEARCH.md

@src/i18n/routing.ts
@src/data/services.ts
@src/data/sectors.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['tr', 'en', 'fr', 'ru'],
  defaultLocale: 'tr',
  localeDetection: true,
  localePrefix: 'always',
});
```

From src/data/services.ts:
```typescript
export interface ServiceItem { slug: string; iconName: string; image: string; featureKeys: string[]; color: string; }
export const services: ServiceItem[];
```

From src/data/sectors.ts:
```typescript
export interface SectorItem { slug: string; iconName: string; image: string; productKeys: string[]; }
export const sectors: SectorItem[];
```

NOTE: This plan creates src/lib/seo/company-info.ts IF plan 01 hasn't already created it.
The structured-data.ts will import from company-info.ts. If plan 01 runs first, it will exist.
If plan 02 runs first, create the file here (same spec as plan 01's company-info.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install schema-dts, create JSON-LD component and structured data builders</name>
  <files>
    package.json,
    src/lib/seo/json-ld.tsx,
    src/lib/seo/structured-data.ts,
    src/__tests__/json-ld.test.ts
  </files>
  <behavior>
    - Test: JsonLd component renders a script tag with type="application/ld+json"
    - Test: JsonLd output has XSS protection (no raw < characters in output)
    - Test: getOrganizationSchema returns object with @context, @type Organization, name, url, logo, contactPoint
    - Test: getLocalBusinessSchema returns @type LocalBusiness with address in Istanbul, TR
    - Test: getBreadcrumbSchema([{name:'Home',url:'...'},{name:'About',url:'...'}]) returns BreadcrumbList with 2 ListItems, positions 1 and 2
    - Test: getFAQSchema([{question:'Q1',answer:'A1'}]) returns FAQPage with mainEntity containing Question+Answer
    - Test: getOrganizationSchema output has availableLanguage including Turkish, English, French, Russian
  </behavior>
  <action>
    1. Install schema-dts: `npm install schema-dts`

    2. Create `src/lib/seo/json-ld.tsx` (SERVER component -- NO 'use client'):
       ```
       import type { Thing, WithContext } from 'schema-dts';

       interface JsonLdProps {
         data: WithContext<Thing>;
       }

       export function JsonLd({ data }: JsonLdProps) {
         return (
           <script
             type="application/ld+json"
             dangerouslySetInnerHTML={{
               __html: JSON.stringify(data).replace(/</g, '\\u003c'),
             }}
           />
         );
       }
       ```
       This is a server component with zero client cost. The replace prevents XSS per Next.js official docs.

    3. Create `src/lib/seo/structured-data.ts`:
       - Import types from schema-dts: WithContext, Organization, LocalBusiness, BreadcrumbList, FAQPage
       - Import COMPANY_INFO from ./company-info (create if not exists)
       - Export getOrganizationSchema(locale: string): Organization schema with Toko Trading info, contactPoint with availableLanguage, sameAs empty array
       - Export getLocalBusinessSchema(locale: string): LocalBusiness with locale-aware name, Istanbul address, geo coordinates (41.0082, 28.9784), opening hours Mon-Fri 09-18
       - Export getBreadcrumbSchema(items: {name:string, url:string}[]): BreadcrumbList with positioned ListItems
       - Export getFAQSchema(questions: {question:string, answer:string}[]): FAQPage with mainEntity of Question+Answer pairs
       - Locale-aware name map: tr: 'Toko Trading - Uluslararasi Ticaret', en: 'Toko Trading - International Trade', fr: 'Toko Trading - Commerce International', ru: 'Toko Trading - Mezhdunarodnaya Torgovlya' (use Cyrillic: 'Международная Торговля')

    4. If `src/lib/seo/company-info.ts` does not already exist (plan 01 may not have run yet), create it with COMPANY_INFO constant: { name: 'Toko Trading', url: 'https://toko.com.tr', phone: '+90-XXX-XXX-XXXX', address: { city: 'Istanbul', country: 'TR' }, social: [], availableLanguages: ['Turkish', 'English', 'French', 'Russian'] }

    5. Create `src/__tests__/json-ld.test.ts`:
       - Test JsonLd renders script tag (use @testing-library/react renderToString or just test the structured-data builder functions)
       - Test all 4 schema builder functions return correct @context and @type
       - Test getBreadcrumbSchema positioning
       - Test getFAQSchema structure
       - Test XSS protection in JSON.stringify output
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/json-ld.test.ts -x</automated>
  </verify>
  <done>
    - schema-dts installed
    - JsonLd component is a server component with XSS protection
    - 4 schema builder functions exported and tested
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sitemap.ts and robots.ts with multilingual coverage</name>
  <files>
    src/app/sitemap.ts,
    src/app/robots.ts,
    src/__tests__/sitemap.test.ts,
    src/__tests__/robots.test.ts
  </files>
  <behavior>
    - Test: sitemap() returns entries for all static pages (11: home, about, services, sectors, references, contact, quote, sourcing, callback, russia-transit, blog)
    - Test: sitemap() returns entries for all 4 service detail pages
    - Test: sitemap() returns entries for all 8 sector detail pages
    - Test: Total sitemap entries = 11 + 4 + 8 = 23 entries
    - Test: Each sitemap entry has alternates.languages with keys tr, en, fr, ru
    - Test: Homepage entry has priority 1.0
    - Test: robots() returns rules allowing / and disallowing /api/
    - Test: robots() includes sitemap URL pointing to https://toko.com.tr/sitemap.xml
  </behavior>
  <action>
    1. Create `src/app/sitemap.ts`:
       - Import MetadataRoute from 'next'
       - Import routing from '@/i18n/routing'
       - Import services from '@/data/services'
       - Import sectors from '@/data/sectors'
       - Define BASE_URL = 'https://toko.com.tr' (or import from seo/metadata if available)
       - Create helper makeEntry(path, priority, changeFreq) that returns a sitemap entry with:
         - url: BASE_URL + '/tr' + path (Turkish as canonical/primary)
         - lastModified: new Date()
         - changeFrequency
         - priority
         - alternates.languages: Object.fromEntries(routing.locales.map(l => [l, BASE_URL + '/' + l + path]))
       - Static pages array with paths and priorities:
         - '' (homepage): priority 1.0, changeFreq 'weekly'
         - '/about': 0.8
         - '/services': 0.9
         - '/sectors': 0.9
         - '/references': 0.7
         - '/contact': 0.8
         - '/quote': 0.7
         - '/sourcing': 0.7
         - '/callback': 0.6
         - '/russia-transit': 0.8
         - '/blog': 0.7, changeFreq 'weekly'
       - Dynamic pages: services.map(s => `/services/${s.slug}`) at priority 0.8
       - Dynamic pages: sectors.map(s => `/sectors/${s.slug}`) at priority 0.8
       - Return [...staticEntries, ...serviceEntries, ...sectorEntries]

    2. Create `src/app/robots.ts`:
       - Import MetadataRoute from 'next'
       - Return: rules: [{ userAgent: '*', allow: '/', disallow: ['/api/'] }], sitemap: 'https://toko.com.tr/sitemap.xml', host: 'https://toko.com.tr'

    3. Create `src/__tests__/sitemap.test.ts`:
       - Import sitemap from app/sitemap (adjust path for test resolution)
       - Test total entry count = 23
       - Test each entry has alternates.languages with 4 locale keys
       - Test homepage priority = 1.0
       - Test all service slugs present
       - Test all sector slugs present
       - Test each alternate URL contains the correct locale prefix

    4. Create `src/__tests__/robots.test.ts`:
       - Import robots from app/robots
       - Test rules include allow '/'
       - Test rules include disallow '/api/'
       - Test sitemap URL is correct
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/sitemap.test.ts src/__tests__/robots.test.ts -x</automated>
  </verify>
  <done>
    - sitemap.ts covers all 23 page paths with hreflang alternates for 4 locales
    - robots.ts properly configured
    - All tests pass
    - `npx next build` succeeds
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/json-ld.test.ts src/__tests__/sitemap.test.ts src/__tests__/robots.test.ts` all pass
- `npx next build` succeeds
- After build: check that sitemap.xml and robots.txt are generated in .next/server/app/
</verification>

<success_criteria>
JSON-LD component and schema builders are ready for integration into pages (plan 03 will wire them). Sitemap covers all 23 pages x 4 locales. Robots.txt is properly configured. All tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-infrastructure/04-02-SUMMARY.md`
</output>
