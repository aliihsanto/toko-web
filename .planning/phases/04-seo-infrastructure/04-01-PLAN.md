---
phase: 04-seo-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/metadata.ts
  - src/lib/seo/company-info.ts
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/about/page.tsx
  - src/app/[locale]/services/page.tsx
  - src/app/[locale]/services/[slug]/page.tsx
  - src/app/[locale]/sectors/page.tsx
  - src/app/[locale]/sectors/[slug]/page.tsx
  - src/app/[locale]/references/page.tsx
  - src/app/[locale]/russia-transit/page.tsx
  - src/app/[locale]/contact/page.tsx
  - src/app/[locale]/quote/page.tsx
  - src/app/[locale]/sourcing/page.tsx
  - src/app/[locale]/callback/page.tsx
  - src/app/[locale]/blog/page.tsx
  - src/messages/tr.json
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/ru.json
  - src/__tests__/seo-metadata.test.ts
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "Every page has locale-aware title and description in <head>"
    - "Every page has correct Open Graph tags (og:title, og:description, og:url, og:locale, og:site_name)"
    - "Every page has canonical URL and hreflang alternates including x-default"
    - "metadataBase is set once in root layout so all relative URLs resolve correctly"
  artifacts:
    - path: "src/lib/seo/metadata.ts"
      provides: "Shared SEO helper functions: BASE_URL, LOCALE_TO_OG map, getAlternates(), getPageMetadata()"
      exports: ["BASE_URL", "LOCALE_TO_OG", "getAlternates", "getPageMetadata"]
    - path: "src/lib/seo/company-info.ts"
      provides: "Company constants: name, url, phone, address, social links for structured data and metadata"
      exports: ["COMPANY_INFO"]
    - path: "src/__tests__/seo-metadata.test.ts"
      provides: "Tests for SEO metadata helpers and per-page metadata output"
  key_links:
    - from: "src/app/[locale]/*/page.tsx"
      to: "src/lib/seo/metadata.ts"
      via: "import getAlternates, getPageMetadata"
      pattern: "import.*from.*@/lib/seo/metadata"
    - from: "src/lib/seo/metadata.ts"
      to: "src/i18n/routing.ts"
      via: "import routing for locale list"
      pattern: "import.*routing.*from.*@/i18n/routing"
---

<objective>
Add generateMetadata to every page in the application with locale-aware title, description, Open Graph tags, canonical URLs, and hreflang alternates.

Purpose: SEO-01 requires every page to have proper metadata for search engine discovery. Currently only 3 files have generateMetadata (layout, services/[slug], sectors/[slug]). The remaining 10+ pages inherit only the layout defaults with no page-specific titles or descriptions.

Output: All pages export generateMetadata with locale-specific content, shared SEO helpers reduce duplication, and translation files contain SEO-specific keys for all pages in all 4 languages.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-infrastructure/04-RESEARCH.md

@src/app/[locale]/layout.tsx
@src/i18n/routing.ts
@src/messages/tr.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SEO helpers, company info constants, and add generateMetadata to all pages</name>
  <files>
    src/lib/seo/metadata.ts,
    src/lib/seo/company-info.ts,
    src/app/[locale]/layout.tsx,
    src/app/[locale]/page.tsx,
    src/app/[locale]/about/page.tsx,
    src/app/[locale]/services/page.tsx,
    src/app/[locale]/services/[slug]/page.tsx,
    src/app/[locale]/sectors/page.tsx,
    src/app/[locale]/sectors/[slug]/page.tsx,
    src/app/[locale]/references/page.tsx,
    src/app/[locale]/russia-transit/page.tsx,
    src/app/[locale]/contact/page.tsx,
    src/app/[locale]/quote/page.tsx,
    src/app/[locale]/sourcing/page.tsx,
    src/app/[locale]/callback/page.tsx,
    src/app/[locale]/blog/page.tsx,
    src/__tests__/seo-metadata.test.ts
  </files>
  <behavior>
    - Test: getAlternates('tr', '/about') returns canonical with full URL and languages object with all 4 locales plus x-default pointing to /tr
    - Test: getAlternates('en', '') returns correct root-level alternates
    - Test: LOCALE_TO_OG maps tr->tr_TR, en->en_US, fr->fr_FR, ru->ru_RU
    - Test: BASE_URL equals 'https://toko.com.tr'
    - Test: COMPANY_INFO has name, url, phone, address fields
  </behavior>
  <action>
    1. Create `src/lib/seo/company-info.ts`:
       - Export COMPANY_INFO constant with: name ('Toko Trading'), url ('https://toko.com.tr'), phone (placeholder '+90-XXX-XXX-XXXX'), address ({ street: placeholder, city: 'Istanbul', country: 'TR', postalCode: placeholder }), social (empty array -- will be filled when accounts are known), availableLanguages: ['Turkish', 'English', 'French', 'Russian']

    2. Create `src/lib/seo/metadata.ts`:
       - Export BASE_URL = 'https://toko.com.tr'
       - Export LOCALE_TO_OG: Record<string, string> mapping tr->tr_TR, en->en_US, fr->fr_FR, ru->ru_RU
       - Export getAlternates(locale: string, path: string) returning { canonical, languages } with all 4 locale URLs plus 'x-default' pointing to /tr path
       - Import routing from @/i18n/routing to enumerate locales dynamically

    3. Update `src/app/[locale]/layout.tsx` generateMetadata:
       - Add metadataBase: new URL(BASE_URL)
       - Add openGraph: { siteName: 'Toko Trading', type: 'website' }
       - Add robots: { index: true, follow: true, googleBot: { index: true, follow: true, 'max-video-preview': -1, 'max-image-preview': 'large', 'max-snippet': -1 } }
       - Replace hardcoded alternates with getAlternates(locale, '') call
       - Keep existing title template pattern

    4. Add generateMetadata to EVERY page that lacks it. Each page's generateMetadata must:
       - Await params to get locale
       - Use getTranslations with locale and page-specific namespace
       - Return title from translation key (page-specific SEO title)
       - Return description from translation key (page-specific SEO description)
       - Return alternates via getAlternates(locale, '/page-path')
       - Return openGraph with title, description, url, locale (from LOCALE_TO_OG), type: 'website'

    Pages needing NEW generateMetadata (10 pages):
    - homepage (page.tsx): namespace 'Metadata', path ''
    - about/page.tsx: namespace 'AboutPage', path '/about'
    - services/page.tsx: namespace 'ServicesPage', path '/services'
    - sectors/page.tsx: namespace 'SectorsPage', path '/sectors'
    - references/page.tsx: namespace 'ReferencesPage', path '/references'
    - russia-transit/page.tsx: namespace 'RussiaTransitPage', path '/russia-transit'
    - contact/page.tsx: namespace 'ContactPage', path '/contact'
    - quote/page.tsx: namespace 'QuotePage', path '/quote'
    - sourcing/page.tsx: namespace 'SourcingPage', path '/sourcing'
    - callback/page.tsx: namespace 'CallbackPage', path '/callback'
    - blog/page.tsx: namespace 'BlogPage', path '/blog'

    Pages needing ENHANCED generateMetadata (2 pages):
    - services/[slug]/page.tsx: Add alternates via getAlternates(locale, `/services/${slug}`), add openGraph fields
    - sectors/[slug]/page.tsx: Add alternates via getAlternates(locale, `/sectors/${slug}`), add openGraph fields

    5. Add SEO translation keys to all 4 message files (tr.json, en.json, fr.json, ru.json):
       - For pages that already have 'hero.title' or similar, reuse those keys in generateMetadata
       - For pages that need dedicated SEO keys, add 'seo.title' and 'seo.description' under the page namespace
       - Each page needs a concise, keyword-rich meta title and a 150-160 char meta description in the correct language
       - Homepage can use existing Metadata.title and Metadata.description
       - About page: use AboutPage.hero.title for title, AboutPage.overview.p1 for description (or add dedicated seo keys if the existing text is too long)
       - Form pages (contact, quote, sourcing, callback): Add seo.title and seo.description keys under their respective namespaces

    6. Create `src/__tests__/seo-metadata.test.ts`:
       - Test getAlternates returns correct structure
       - Test LOCALE_TO_OG completeness
       - Test BASE_URL value
       - Test COMPANY_INFO structure

    IMPORTANT: The homepage (page.tsx) generateMetadata should use the existing 'Metadata' namespace (already has title/description). For the homepage, the title should be the default (will use layout template's default), so return just description and alternates -- or return title: null to use layout default.

    For pages that use getTranslations with 'hero.title' -- the meta title should be shorter and keyword-optimized. Add 'seo.title' and 'seo.description' keys if 'hero.title' is too long/generic for SEO.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/seo-metadata.test.ts -x && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 13+ page files export generateMetadata
    - Every generateMetadata includes locale-aware title, description, openGraph, and alternates
    - metadataBase set in layout, x-default hreflang pointing to /tr
    - SEO translation keys exist in all 4 locale files
    - Tests pass, build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/seo-metadata.test.ts` passes
- `npx next build` succeeds without errors
- Grep confirms all page.tsx files export generateMetadata: `grep -r "generateMetadata" src/app/[locale]/ --include="page.tsx" -l` returns all page files
</verification>

<success_criteria>
Every page in the application has locale-specific title, description, OG tags, canonical URL, and hreflang alternates. The metadata helper functions are reusable and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-infrastructure/04-01-SUMMARY.md`
</output>
